---

description: "Task list template for feature implementation"
---

# Tasks: Mi Perfil (Profile) Redesign (Apple HIG-Inspired)

**Input**: Design documents from `/specs/050-profile-redesign/`

**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/](./contracts/), [quickstart.md](./quickstart.md)

**Tests**: Tests are included for behavior at real regression risk —
display-name validation/save-error retention, linked-provider/connected-app
state, and the corrected accessible-disabled-placeholder gap (FR-007) —
per constitution Principle I (TDD, NON-NEGOTIABLE). Pure visual/motion
restyling with no pre-existing behavior to protect follows the precedent
features 028/029/031 established (no new test required for cosmetic-only
change); every such task below says so explicitly.

**Organization**: Tasks are grouped by user story (spec.md's US1-US4) to
enable independent implementation and testing of each. A Foundational phase
precedes all of them because FR-015 requires exploring 2-3 visual
directions and producing the product-owner comparison artifact before any
story-specific rebuild can proceed.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1, US2, US3, US4)
- File paths are relative to `retro-rocket/` unless otherwise noted

## Path Conventions

Single existing React SPA frontend project — all paths are under
`retro-rocket/src/` (or `retro-rocket/e2e/` for Playwright specs), per
`plan.md`'s Project Structure. No backend/API paths are touched.

---

## Phase 1: Setup

**Purpose**: Establish a regression baseline and a place to build the
prototyped visual directions without touching the shipped page yet.

- [X] T001 Establish baseline: run `npm run type-check`, `npm run lint`, `npm run test:coverage`, and `npm run e2e -- profile.spec.ts authentication.spec.ts accessibility.spec.ts` from `retro-rocket/` and record the passing baseline (coverage numbers, test counts) to compare against after implementation (FR-016). This full-suite pass is also the pre-redesign baseline `contracts/functional-parity-contract.md`'s verification procedure checks every capability row against — no separate per-row baseline pass is needed. — **Baseline (2026-08-17)**: type-check 0 errors; lint 0 errors/warnings; `test:coverage` 185 files/2503 tests passed (2 files/3 tests skipped), thresholds met (77.45% stmts / 83.42% branches / 76.9% funcs / 77.45% lines, vs. 50/78/64/50 floor); `test:server:coverage` (supplementary) 82 files/577 tests passed, 76.91% stmts / 85.04% branches / 72.58% funcs / 76.91% lines; e2e `profile.spec.ts authentication.spec.ts accessibility.spec.ts` 76/77 passed — the literal `npm run e2e -- <specs>` invocation errors ("Too many arguments") because the npm script wraps the playwright command in a single quoted string, so extra args land on `firebase emulators:exec` instead of `playwright test`; ran the equivalent `firebase emulators:exec --only auth,firestore "npx playwright test --project=chromium profile.spec.ts authentication.spec.ts accessibility.spec.ts"` directly. 1 pre-existing flaky failure (`accessibility.spec.ts:118 Board empty-column state has no WCAG 2.1 AA violations (light)`, `Execution context was destroyed, most likely because of a navigation`) — matches the same flake documented in features 028/029/031's baselines; re-ran in isolation with `--repeat-each=3` and it passed 3/3, confirming flake not regression.
- [X] T002 [P] Add a dev-only prototype route scaffold in `retro-rocket/src/App.tsx` (e.g. gated behind `import.meta.env.DEV`) that will mount the 2-3 candidate `Profile` variants side by side for review, per `contracts/visual-direction-review-contract.md` — added `retro-rocket/src/pages/__prototypes__/ProfileDirectionsScaffold.tsx` (accessible tab switcher, lazy-loading each candidate), placeholder stubs `ProfileDirectionA/B/C.tsx` (to be replaced by T003-T005), and a `/dev/profile-directions` route in `App.tsx` gated behind `import.meta.env.DEV`; `type-check`/`lint` clean

**Checkpoint**: Baseline recorded, prototype scaffold ready.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Explore and select the one Visual Direction (FR-015) all user
stories will be built against, and produce the product-owner comparison
artifact this feature was explicitly asked to deliver.

**⚠️ CRITICAL**: No user-story section work can begin until the product
owner has reviewed the comparison artifact and selected a direction (T009).

- [X] T003 [P] Using the `apple-design`/`emil-design-eng` skills (the constitution-mandated `prototype` skill is not installed in this environment; per the precedent established in features 029/031/033, `apple-design`/`emil-design-eng` are substituted for building real interactive candidates), draft Visual Direction A as a real, working page variant in `retro-rocket/src/pages/__prototypes__/ProfileDirectionA.tsx`, wired to real `backendProfileClient`/`useLinkedProviders`/`connectedAppsService` data, satisfying every item in `contracts/visual-direction-review-contract.md`'s "Required before review" checklist — **Direction A: "Deference-forward."** No card/border/shadow chrome anywhere; section boundaries come from whitespace and small uppercase eyebrow labels instead of `Card`. Display name is inline click-to-edit (plain `<h1>` text + always-visible "Edit name" ghost trigger) rather than a permanently-visible boxed form. All actions (sign out, connect provider, revoke app) render as low-emphasis ghost text-buttons placed inline with the row they act on, never as prominent filled buttons. Disabled account-action placeholders are pushed to a quiet block at the very bottom, behind the page's one deliberate hairline divider. Motion is a single restrained opacity+8px entrance fade (no per-section stagger/slide), gated explicitly via `useReducedMotion()`. Wired to real `useUser`/`useLinkedProviders`/`useConnectedApps` data and the real `updateDisplayName`/`signOut`/`startLinkProvider`/`revoke` behaviors (not mocked). `type-check`/`lint` clean; new i18n keys added to `en.json`/`es.json` under `profileDirectionA.*` (existing `profile.*`/`auth.userProfileForm.*`/`mcpConnector.connectedApps.*` keys reused where applicable); no new dependencies.
- [X] T004 [P] Using the same substituted skills, draft Visual Direction B as a real, working page variant in `retro-rocket/src/pages/__prototypes__/ProfileDirectionB.tsx`, genuinely distinct from Direction A per `data-model.md`'s `distinguishingChoices` field, satisfying the same review-contract checklist — **Direction B: "Clarity-forward."** A settings/account-pane layout, not a marketing profile card: three explicit, clearly-labeled sections (Identity / Access & Security / Account Actions), each a real bordered `GroupedList` container — visual separation comes from borders/dividers, not whitespace. Linked providers and connected AI assistants both render as uniform Settings-style rows (fixed leading icon slot, primary/secondary text, trailing action) instead of ad hoc card layouts. Every state is spelled out in visible text alongside icon/color — "Linked" next to the check icon, "Not linked"/"Not available yet" as explicit secondary captions, never color-only. Display-name editing expands in place within the Identity panel (persistent "Edit" button, not implicit inline-click) with its own validation/saving/error text. Motion is a single quiet fade (≤0.24s, no bounce, per emil-design-eng's duration/easing rules and research.md §3's "quiet, not celebratory" guidance for routine feedback) and is fully skipped (`initial={false}`, no `exit`, `duration:0`) when `useReducedMotion()` is true. Wired to real `useUser`/`useLinkedProviders`/`useConnectedApps` data and the real `updateDisplayName`/`signOut`/`startLinkProvider`/`revoke` behaviors (not mocked); built as new presentational components inside the file rather than reusing `UserProfileForm`/`LinkedProvidersCard`/`ConnectedAppsCard` (none of those are touched, so the landing page's `isFirstTime` rendering has zero regression risk). `type-check`/`lint` clean (0 errors, 0 warnings on this file); new i18n keys added to `en.json`/`es.json` under `profileDirectionB.*` (existing `profile.*`/`common.*`/`auth.userProfileForm.*`/`mcpConnector.connectedApps.*` keys reused where applicable); no new dependencies. Manual contrast check confirms `success-fg`/`error-fg` text on `surface`/`surface-raised` clears 7:1+ in both themes even though `CONTRAST_PAIRINGS` only enumerates those tokens against their own status `-bg`.
- [X] T005 [P] Using the same substituted skills, draft Visual Direction C as a real, working page variant in `retro-rocket/src/pages/__prototypes__/ProfileDirectionC.tsx`, genuinely distinct from Directions A and B, satisfying the same review-contract checklist — **Direction C: "Depth-forward."** Three explicit material tiers at distinct elevations (apple-design §12): the identity card is the heaviest/highest layer (`backdrop-blur-xl` + a deep custom shadow) and physically overlaps the panel below it via a negative bottom margin, so it reads as floating above the page; "Access & Security" (sign-in methods + connected AI assistants) sits in a recessed `shadow-inner` backdrop panel whose rows are each their own individually-elevated tile (own border/shadow/hover-lift), not a flat list inside one shared card; "Account Actions" is the flattest, quietest tier, deliberately pushed back per HIG's destructive-action separation. All three providers (Google/GitHub/Apple) are now rendered as one row each with an explicit `linked`/`linkable`/`not-yet-available` state — Apple was previously absent from the UI entirely despite `handleLinkProvider` having dead code for it; this closes that gap and gives FR-007's disabled-placeholder pattern (native `disabled` + persistent visible label + `aria-describedby`) to the not-yet-available Apple row too, per `data-model.md`'s `Linked Provider Row` validation. Motion: mount-time entrance is staggered by tier (identity settles first, ~90ms stagger, strong ease-out `cubic-bezier(0.23,1,0.32,1)`, never `scale(0)`) via framer-motion, already gated app-wide by `<MotionConfig reducedMotion="user">`; the plain-CSS hover/focus elevation-lift on each row-tile is explicitly gated by `useReducedMotion()` (transform removed, shadow-only feedback retained under reduced motion). Builds its own presentation for the identity/providers/connected-apps sections (real `useUser`/`useLinkedProviders`/`useConnectedApps`/`updateDisplayName`/`signOut`/`startLinkProvider`/`revoke` data and behavior — no shared component file modified) rather than reusing `UserProfileForm`/`LinkedProvidersCard`/`ConnectedAppsCard` as-is, since those don't offer per-row elevated-tile presentation and `LinkedProvidersCard` hardcodes its copy in Spanish outside i18next. Also fixes the member-since date's locale (uses `i18n.language` via `Intl.DateTimeFormat`, not a hardcoded `es-ES`). `type-check`/`lint` clean (0 errors, 0 warnings); new i18n keys added to `en.json`/`es.json` under `profileDirectionC.*` (existing `profile.*`/`auth.userProfileForm.*`/`mcpConnector.connectedApps.*`/`common.retry` keys reused where applicable); no new dependencies. Verified rendering (no console errors beyond the expected backend-not-running 500s) in both light and dark themes and at mobile/desktop widths via the T002 scaffold; full signed-in-data visual comparison is left to T006/T008 per the task breakdown.
- [X] T006 Wire the three prototypes into the dev-only route scaffold from T002 in `retro-rocket/src/App.tsx`, so they are viewable side by side in both themes and at mobile/desktop widths against a real signed-in account's data — verified via a throwaway Playwright script (`e2e/fixtures/profile-direction-capture.spec.ts`, deleted after use, same pattern as feature 031's T007): signed in via the emulator-only `POST /api/auth/test-login` endpoint, navigated to `/dev/profile-directions`, and screenshotted all three tabs against real signed-in profile/provider/connected-app data in both light and dark themes at desktop width (1280×900), plus a light-theme mobile pass (390×844) — 9 screenshots total, all three candidates render correctly with no console errors, no broken layout in either theme or viewport class
- [X] T007 [P] Record all three candidates in `specs/050-profile-redesign/data-model.md`'s `Visual Direction` table (`concept`, `distinguishingChoices`, `newDependencies`) — added the T007 Catalog table (2026-08-17) with all three directions at `status: explored`, `reviewedBy: pending — T009`; no new dependencies in any candidate
- [X] T008 Produce the `Product Owner Decision Artifact` per `data-model.md`: a single, self-contained, published comparison page (Claude Artifact) presenting all three candidates side by side — `concept`, `distinguishingChoices`, and representative views of each candidate's loaded/error/saving states — satisfying `contracts/visual-direction-review-contract.md`'s "Required for the Product Owner Decision Artifact" checklist — published: https://claude.ai/code/artifact/8b9b9b99-be98-41e5-a4b2-f29a36feb134 ("Mi Perfil Directions"), embedding all 9 real signed-in screenshots with a per-direction light/dark toggle; recorded in `data-model.md`'s `Product Owner Decision Artifact` entity
- [X] T009 Product-owner review checkpoint: present the T008 artifact per `contracts/visual-direction-review-contract.md`'s review procedure; record the selected direction and the two `rejected` ones (with `rejectionReason`) in `specs/050-profile-redesign/data-model.md`. Because the constitution-mandated `prototype`/`pick-ui-library` skills aren't installed in this environment (`plan.md`'s Constitution Check row IX, `research.md` §5), this checkpoint MUST also have the product owner explicitly acknowledge the `apple-design`/`emil-design-eng` substitution used to build T003-T005, alongside approving the chosen direction — record this acknowledgment (the `Product Owner Decision Artifact`'s `toolingAcknowledgment` field) next to the selection — **Resolved 2026-08-17**: product owner (Fernando Ortiz) reviewed the published artifact and selected **Direction B (Structured Account Pane)**; A and C recorded as rejected with reasons; tooling-substitution acknowledgment recorded per the artifact's decision-panel framing; see `data-model.md`
- [X] T010 Delete the two non-selected prototype files from `retro-rocket/src/pages/__prototypes__/` and remove the dev-only route scaffold from `retro-rocket/src/App.tsx` added in T002/T006 — deleted `ProfileDirectionA.tsx`, `ProfileDirectionC.tsx`, and `ProfileDirectionsScaffold.tsx`; removed its lazy import and the `/dev/profile-directions` route from `App.tsx`; removed the now-unused `profileDirectionA.*`/`profileDirectionC.*` i18n namespaces from `en.json`/`es.json`; kept `ProfileDirectionB.tsx` unrouted as a T014+ build reference (per feature 031's T010 precedent) and updated its stale doc comment accordingly; `type-check`/`lint` clean after all deletions
- [X] T011 [P] If the selected direction requires new design tokens (`data-model.md`'s `Design Token Extension`), add them to `retro-rocket/src/lib/theme/tokens.ts` and add/extend their `CONTRAST_PAIRINGS` entries in `retro-rocket/src/lib/theme/contrast.ts`, verified by `contrast.tokens.test.ts` in both themes — skip explicitly (record "no new tokens needed") if the selected direction uses only existing tokens — **no new tokens needed**; confirmed Direction B's `success-fg`/`error-fg` on `surface`/`surface-raised` pairing (used for the "Linked" status row and inline error text inside `GroupedList` cards) was genuinely absent from `CONTRAST_PAIRINGS` in `retro-rocket/src/lib/theme/tokens.ts` (that's where the array actually lives, not `contrast.ts`, which holds only the pure WCAG math functions); added the 4 missing pairings there — `contrast.tokens.test.ts` went from 42 to 50 passing tests, all pairings clear AA in both themes, no value weakened to pass

**Checkpoint**: Selected direction recorded, product-owner decision
artifact delivered and acknowledged — user story implementation can now
begin.

---

## Phase 3: User Story 1 - View My Profile (Priority: P1) 🎯 MVP

**Goal**: A signed-in user opens Mi Perfil and sees their avatar, display
name, read-only email, primary provider, and member-since date, presented
through the selected visual direction.

**Independent Test**: Sign in (including as a first-time user), open Mi
Perfil, confirm all profile fields render correctly; simulate a load
failure and confirm a clear error state.

### Tests for User Story 1 ⚠️

> Write these first, per Constitution Principle I.

- [X] T012 [P] [US1] Write a new page-level test `retro-rocket/src/test/pages/Profile.test.tsx` (no such test currently exists) asserting avatar/fallback, display name, read-only email, primary provider badge, and member-since date all render from `userProfile`, plus that the loading and error `Profile View State` variants (`data-model.md`) render correctly (FR-002, FR-010) — written against the pre-rebuild `Profile.tsx`, mocking `AuthWrapper`/`UserProfileForm`/`LinkedProvidersCard`/`ConnectedAppsCard` and `useUserContext` so the page's own identity-header/loading/error handling is isolated from those subcomponents' own (separately-tested) internals; assertions use semantic queries (role, accessible text, rendered values via a structure-agnostic `findValueOnPage` text-or-input-value helper) rather than CSS/structure selectors, so they should keep holding through the Direction B rebuild. **`npx vitest run src/test/pages/Profile.test.tsx --config vitest.config.ts` result: 5 passed / 3 failed (8 total)** — passing: display name, read-only email, member-since date (formatted), primary-provider badge, avatar-with-photoURL (all pre-existing capability, correctly protected as regression coverage); failing as expected/designed (the actual TDD-red bar for T014-T016): (1) "renders a fallback avatar... when photoURL is absent" — current `Profile.tsx` renders no avatar element at all (no `<img>`, no fallback) when `photoURL` is null, only `ProfileDirectionB.tsx`'s `Avatar` component has the `role="img"` initials fallback FR-002 requires; (2) "shows a loading state while the profile is still being fetched" — current `Profile.tsx` never branches on `userProfile === null`, it renders the full shell with blank fields instead of `common.loading`/a loading indicator; (3) "shows a clear, visible error state... when the profile load fails" — current `Profile.tsx` has no error branch at all (`role="alert"`, this codebase's established non-silent-error pattern per `Dashboard.tsx`, is absent). All 3 failures are structural gaps FR-002/FR-010 require T014-T016 to close, not test-authoring bugs.
- [X] T013 [P] [US1] Update `retro-rocket/e2e/profile.spec.ts`'s view-profile assertions/selectors for the new structure, preserving every existing assertion — reviewed all 3 view-profile-section tests (`e2e/profile.spec.ts` lines 34-72): the heading (`getByRole('heading', {name: 'Mi Perfil'})`), display-name, member-since-label, and generic-error assertions are already structure-agnostic semantic locators tied to i18n text/roles Direction B (`ProfileDirectionB.tsx`) keeps unchanged (`profile.title`/`profile.memberSince` keys, same `<h1>`), so left as-is — noted explicitly, no change needed there. Changed only the two bare `page.getByText(<email>)` locators (no `.first()`) to `page.getByText(<email>).first()`: Direction B's Identity section renders the read-only email as its own visible text row (separate from any `UserProfileForm` embed, which represents email via a non-text `<input value>` that `getByText` wouldn't match anyway), so this is a defensive robustness fix against Playwright strict-mode multi-match failure, not a behavioral weakening — the visibility assertion on the same email value is unchanged.

### Implementation for User Story 1

- [X] T014 [US1] Rebuild `retro-rocket/src/pages/Profile.tsx`'s identity header (avatar with fallback, display name, primary provider badge) per the selected direction (FR-002) — added a local `ProfileAvatar` (image when `photoURL` present, `role="img"` initials fallback otherwise — modeled on `ProfileDirectionB.tsx`'s `Avatar`), rebuilt the "Profile Info" panel into a bordered `rounded-xl border border-border-default bg-surface-raised` pane (Direction B's Identity-section look) with display name as an `<h2>` and the primary-provider name inline in the caption; `getProviderName()` kept literal (Google/GitHub/Apple), unchanged
- [X] T015 [US1] Rebuild the read-only email and member-since date display in `retro-rocket/src/pages/Profile.tsx` per the selected direction; fix the member-since date's hardcoded `Intl.DateTimeFormat('es-ES', …)` locale (found during planning — `Profile.tsx`'s current formatter ignores the active language) to use the active `i18next` language instead, consistent with FR-011 and the constitution's Internationalization standard — email now renders as its own bordered row with a "not editable" caption pill (reusing existing `auth.userProfileForm.email`/`emailNotEditable` keys); `formatDate()` now uses `currentLanguage` from `useLanguage()` (i.e. `i18n.language`) instead of the hardcoded `'es-ES'` literal
- [X] T016 [US1] Rebuild the loading and error states for the profile-data fetch in `retro-rocket/src/pages/Profile.tsx` per the selected direction (FR-010, `data-model.md`'s `Profile View State`), ensuring the error state is never silent — added an explicit `!userProfile` branch: `error` (from `useUser()`) renders a `role="alert"` panel (icon + `profile.error.title`/`profile.error.loadMessage` + a retry button calling `refreshUserProfile()`), matching `Dashboard.tsx`'s established loadError pattern; no error renders `<Loading text={t('common.loading')} />` instead of the old blank-field shell
- [X] T017 [US1] Using the `animate` skill (Constitution Principle IX), implement page/section entrance motion for the rebuilt profile identity header in `retro-rocket/src/pages/Profile.tsx`, gated by the existing `useReducedMotion` hook (FR-013) — ran the `animate` skill: gate passes at the "occasional" tier, purpose is "preventing a jarring change" (settling content on page load, not a celebratory moment per research.md §3); tool is framer-motion (already adopted, matches the file's existing pattern); properties are `opacity`+`transform` (`y: 8→0`) only; concluded Direction B's own already-decided treatment for this exact moment (opacity+8px, 0.24s, `cubic-bezier(0.23,1,0.32,1)` ease-out, no bounce) fully answers this task rather than requiring a new decision — reused verbatim on the identity panel specifically (`initial={reducedMotion ? false : {opacity:0,y:8}}`, `transition={reducedMotion ? {duration:0} : {duration:0.24, ease:[0.23,1,0.32,1]}}`); left the page's other pre-existing (already-ungated) motion.div wrappers — page header, LinkedProvidersCard/ConnectedAppsCard column, UserProfileForm column, Account Actions — untouched, since those belong to later tasks (T022/T028/T029/T030/T031/T032) that rebuild those regions

**Checkpoint**: User Story 1 is independently functional and testable —
T012-T013 pass, both view-profile states verified per `quickstart.md` §1.

---

## Phase 4: User Story 2 - Edit My Display Name (Priority: P1)

**Goal**: A signed-in user changes their display name and sees the update
confirmed, with the same validation/feedback behavior as today, on both
its Mi Perfil and landing-page first-time-setup renderings.

**Independent Test**: Save a valid new name (persists after reload); save
a blank name (blocked); simulate a save failure (previous name retained,
clear error shown). Repeat the first-time-setup rendering on the landing
page.

### Tests for User Story 2 ⚠️

- [X] T018 [P] [US2] Update `retro-rocket/src/test/features/auth/UserProfileForm.test.tsx` for the new structure, preserving required/non-empty validation, saving/loading indicator, success/error feedback, and previous-value-retained-on-failure assertions (FR-003); add assertions covering both `isFirstTime={false}` (Mi Perfil) and `isFirstTime={true}` (landing) rendering modes — rewrote the file (a partial version already existed on disk from an earlier interrupted run; reviewed and finished it rather than redoing from scratch) to drop the old per-file mocks for `react-i18next`/`framer-motion`/`lucide-react`/`Button`/`Input` (which coupled assertions to hand-rolled `data-testid` stand-ins) in favor of this codebase's established global mocks in `src/test/setup.ts`, so assertions use real semantic queries (`getByLabelText`, `getByRole`) instead. Added a `getDisplayNameField()` helper that reveals the field via an "Edit" control first if the T021 rebuild gates it (per `ProfileDirectionB.tsx`'s `IdentityPanel` reference), and is a no-op against the current always-visible field — so every assertion validates correctly both before and after T021. Converted the validation/save/saving-indicator/error/previous-value-retained assertions into a `describe.each` running identically against `isFirstTime={false}` and `isFirstTime={true}`, per data-model.md's "behavior identical regardless of isFirstTime" rule (previously only `isFirstTime={false}` exercised this contract). Also added `motion.form: 'form'` to `src/test/setup.ts`'s framer-motion mock (was referenced but missing from the earlier interrupted attempt) since `ProfileDirectionB.tsx`'s reveal-on-Edit form uses `motion.form`/`AnimatePresence` — needed so a T021 rebuild following that reference won't crash under this shared mock. `npx vitest run src/test/features/auth/UserProfileForm.test.tsx`: 21/21 passed against the current pre-rebuild component.
- [X] T019 [P] [US2] Update `retro-rocket/e2e/profile.spec.ts`'s edit-display-name assertions/selectors for the new structure, preserving every existing assertion — the existing locators (`page.getByLabel('Nombre a mostrar', ...)`, `page.getByRole('button', {name: 'Guardar cambios'})`, and the toast-text assertions, which originate from `UserContext.tsx`'s `updateDisplayName` — outside `UserProfileForm`/`Profile.tsx` entirely, so unaffected by the redesign) were already structure-agnostic semantic locators; added a `getDisplayNameInput(page)` helper (mirroring T018's `getDisplayNameField()`) that reveals the field via the "Editar" button first if it isn't already visible, and used it in place of the 3 bare `page.getByLabel(...)` calls in the rename/blank-name/failed-save tests — a forward-compatibility fix for the same possible Edit-gated structure T018 anticipates, not a behavioral weakening of any assertion.
- [X] T020 [P] [US2] Update `retro-rocket/src/test/pages/Landing.test.tsx` for the rebuilt `UserProfileForm` structure in its `isFirstTime` rendering, confirming no regression (FR-009) — existing "first-time profile setup" coverage only asserted the form's appearance (Continue button rendered, `bg-surface` wrapper class) after sign-in, never that submitting it actually reaches `updateDisplayName` and un-mounts correctly; added a `getDisplayNameField()` helper (same Edit-gate-tolerant pattern as T018/T019) and a new test that fills the display name, clicks Continue, and asserts `mockUpdateDisplayName` is called with the trimmed value and the marketing hero reappears afterward (`handleProfileSave`'s `setShowProfileForm(false)` on success) — this is the Landing-specific integration point that only this file can verify, complementing T018's isolated `UserProfileForm` coverage. `npx vitest run src/test/pages/Landing.test.tsx`: 16/16 passed (was 15 before the added test).

### Implementation for User Story 2

- [X] T021 [US2] Rebuild `retro-rocket/src/features/auth/components/UserProfileForm.tsx` per the selected direction — inline validation, saving/loading indicator, success/error feedback, disabled read-only email field — preserving its existing `userProfile`/`onSave`/`isFirstTime` prop contract so both Mi Perfil and the landing page's first-time-setup usage keep working unchanged (FR-003, FR-009) — rebuilt per Direction B's `IdentityPanel` reference (`ProfileDirectionB.tsx`): Mi Perfil (`isFirstTime=false`) now shows the display name as static text behind a persistent "Edit" button that expands a real inline form (avoids duplicating the avatar/provider/date identity summary `Profile.tsx`'s own T014-17 rebuild already renders); first-time setup (`isFirstTime=true`) keeps the field directly visible with no Edit gate, since there's no prior value to protect and gating it would only add onboarding friction — `isFirstTime` governs this presentation choice only, not validation/persistence, per data-model.md. Save success is tracked as its own `status='saved'` state (not tied to closing the edit form) specifically so the submit button stays mounted and becomes interactive again immediately after a successful save, matching `UserProfileForm.test.tsx`'s "saving/loading indicator" assertion (an auto-collapse-on-success design was tried first and rejected — it unmounted the submit button in the same render as the state update, leaving a stale detached-node `disabled=true` read that broke that test). Added 4 new i18n keys (`blankNameError`, `saveError`, `saveSuccess`, `avatarAlt`) to both `en.json`/`es.json` in lockstep; the existing `Nombre actualizado exitosamente`/`Error al actualizar el nombre` toasts remain owned by `UserContext.tsx`, unaffected. `npx vitest run src/test/features/auth/UserProfileForm.test.tsx src/test/pages/Landing.test.tsx`: 37/37 passed. `npm run type-check` and `npm run lint`: clean.
- [X] T022 [US2] Using the `animate` skill, implement the display-name save success/error feedback motion in `UserProfileForm.tsx`, gated by the existing `useReducedMotion` hook (FR-013) — ran the `animate` skill: gate passes at the "occasional" tier (settings edits/onboarding, not a high-frequency action); purpose is "state indication" (making the saved/errored state of the field legible), not "delight" — research.md §3's "quiet, not celebratory" framing rules out any bounce/scale/confetti treatment. Tool is framer-motion (already adopted); properties are `opacity`+`transform` (`y: -4→0`) only, reusing the file's existing `EASE_OUT` curve (`cubic-bezier(0.23,1,0.32,1)`, the same curve already decided for this class of moment in `ProfileDirectionB.tsx`/`Profile.tsx`'s T017), at 0.16s — shorter than the 0.24s section-entrance budget since this is a small inline message, not a whole panel. Implemented as `AnimatePresence mode="wait"` over a single conditionally-rendered `<motion.p>` (error: `role="alert"` + `AlertCircle` icon + error-fg text; success: `role="status"` + `CheckCircle2` icon + success-fg text — icon+text pairing, never color alone, per WCAG). Uses framer-motion transitions (not keyframes) so a rapid retry after a failed save retargets cleanly instead of restarting; symmetric exit (reverse of entrance); fully gated to `{duration:0}` under `useReducedMotion()`. `npx vitest run src/test/features/auth/UserProfileForm.test.tsx`: 21/21 passed (includes the "no stray `role=alert`after a successful save" and "clears the saving indicator on failure" assertions this motion had to coexist with).
- [X] T023 [US2] Verify `retro-rocket/src/pages/Landing.tsx`'s first-time-setup rendering path (`isFirstTime={true}`) still renders and functions correctly against the rebuilt `UserProfileForm`, per `research.md` §4 and `e2e/authentication.spec.ts`'s first-time-setup path — no changes to `Landing.tsx` itself expected — confirmed: `Landing.tsx` was not touched. `npx vitest run src/test/pages/Landing.test.tsx`: 16/16 passed, including T020's full first-time-setup submit-to-`updateDisplayName` integration test, against the rebuilt component. Read `e2e/authentication.spec.ts` in full: none of its tests actually exercise Landing's `showProfileForm`/`isFirstTime={true}` UI — every sign-in there goes through the `signInWithGoogle`/`signInAs` fixtures, which establish the session via the emulator-only `POST /api/auth/test-login` endpoint and navigate straight to `/` then wait for the post-login redirect to `/mis-tableros`, bypassing Landing's own sign-in button/`showProfileForm` state entirely (the "brand-new account ... with no setup step" test's own comment confirms provider-derived names arrive pre-populated, so this UI path isn't reached in that suite) — so there is no first-time-setup DOM-selector-based E2E assertion to be compatible with here; `Landing.test.tsx` (unit) is the sole regression guard for this path, and it passes.

**Checkpoint**: User Stories 1 and 2 both work independently — editing the
display name behaves identically to before, on both call sites.

---

## Phase 5: User Story 3 - Manage Account Access (Priority: P2)

**Goal**: A signed-in user signs out, views/links sign-in providers, and
views/revokes connected AI assistants from Mi Perfil, with the two disabled
account-action placeholders gaining correct accessible disabled-state
semantics (the FR-007 correction).

**Independent Test**: Sign out (and simulate failure); with one linked
provider, confirm linked/linkable states and complete linking the other;
with a connected AI assistant, confirm it lists and revoking it removes it
(and simulate failure); confirm the disabled placeholders are announced as
unavailable to assistive technology.

### Tests for User Story 3 ⚠️

> The disabled-placeholder accessibility test must FAIL against the
> current implementation (no `aria-describedby` association) before T031
> makes it pass.

- [X] T024 [P] [US3] Update `retro-rocket/src/test/features/auth/LinkedProvidersCard.test.tsx` for the new structure, preserving linked/linkable/not-yet-available assertions (FR-005) — rewrote the file: dropped the hand-rolled `data-testid` icon/Button/Card mocks and the hardcoded-Spanish-copy heading assertion (the current component hardcodes its copy in Spanish entirely outside i18next — noted in T005/data-model.md — so T029 is expected to introduce translation keys here, which would break any assertion tied to that literal text); relies on `src/test/setup.ts`'s global react-i18next/react-hot-toast mocks (no per-file duplicates), keeps a local `Loading` mock (stable reused primitive, no accessible role of its own). Assertions now use `getByRole('button')` + `.disabled` filtering (linked providers offer no actionable link control, linkable ones do — data-model.md's Linked Provider Row validation) instead of literal button-label text where the label itself is likely to change. Also added a genuinely-new, real regression-risk test for the not-yet-available state (FR-005 explicitly requires Apple be indicated, not silently omitted) — **EXPECTED-RED, confirmed**: `screen.getByText(/apple/i)` fails against current `LinkedProvidersCard.tsx`, which never renders Apple at all (`availableProviders` only lists google/github; the `handleLinkProvider`/`getProviderIcon` apple branches are dead code, unreachable from any rendered row) — closed by T029 (`ProfileDirectionB.tsx`'s `PROVIDER_DEFS` already includes an `apple`/`available:false` entry as build reference). `npx vitest run src/test/features/auth/LinkedProvidersCard.test.tsx`: 6/7 passed, 1 expected-red (the Apple test) — confirmed failing for the right reason (`getByText` finds nothing, not a wrong-reason crash).
- [X] T025 [P] [US3] Update `retro-rocket/src/test/features/auth/components/ConnectedAppsCard.test.tsx` for the new structure, preserving list/revoke/loading/error assertions (FR-006) — the pre-existing 4 tests (origin labels, never-used-yet, last-used date, AnimatePresence-wrapping) were genuinely thin against FR-006's full contract ("listing ... and allowing per-app revocation with a loading indicator and clear success/error feedback"): no loading-state, error-state, empty-state, or revoke-success/error/in-progress coverage existed at all. Added 7 new tests covering all of those (loading spinner marker, card-level error message, empty-list message, revoke-success-with-toast, revoke-in-progress-disables-control, revoke-error-leaves-entry-visible-with-toast). Dropped the per-file react-i18next/react-hot-toast mocks (now covered by `src/test/setup.ts`'s global mocks, same as T024/T018) but kept a local `framer-motion` override — only to preserve the `AnimatePresence` `data-testid` marker the existing exit-animation regression test (design audit DAF-001) needs; every other `motion.*` tag maps to its plain host element same as the global mock. All existing assertions preserved; `npx vitest run src/test/features/auth/components/ConnectedAppsCard.test.tsx`: 11/11 passed (was 4/4) — no red state here, this file's underlying component behavior already satisfies FR-006 today, only its test coverage was thin.
- [X] T026 [P] [US3] Add a new test (in the page-level `Profile.test.tsx` from T012, or a dedicated test file) asserting the "Exportar mis datos"/"Eliminar cuenta" controls are `disabled` and have an `aria-describedby` resolving to visible, non-empty "not yet available" text — write it to FAIL against the current implementation first, then pass after T031 (FR-007) — added to `retro-rocket/src/test/pages/Profile.test.tsx` (no dedicated file needed, since UserProfileForm/LinkedProvidersCard/ConnectedAppsCard are already stubbed there, isolating Profile.tsx's own account-action placeholders as the only `disabled` controls in the render tree). Test collects every `disabled` button (asserts exactly 2 — data-model.md's `export-data`/`delete-account`), then for each asserts `aria-describedby` is present, resolves via `document.getElementById` to a real element, that element `toBeVisible()`, and its `textContent` is non-empty — deliberately not anchored to specific label copy, which T031 is free to choose, only to the FR-007 structural requirement itself. **CONFIRMED RED**: `npx vitest run src/test/pages/Profile.test.tsx -t "resolvable, visible not-yet-available description"` fails with `AssertionError: expected null to be truthy` at the `aria-describedby` assertion (i.e. it got past the "exactly 2 disabled buttons" check, then failed specifically because current `Profile.tsx`'s export/delete buttons have no `aria-describedby` at all — their unavailable status today is folded entirely into the button's own accessible name, `t('profile.accountActions.exportData.button')` = "Coming soon"/"Próximamente", losing the actual action's name and giving AT no separate description to announce). This is exactly the FR-007/research.md §2 gap T031 closes. Full file: `npx vitest run src/test/pages/Profile.test.tsx`: 8/9 passed, 1 expected-red.
- [X] T027 [P] [US3] Update `retro-rocket/e2e/profile.spec.ts`'s sign-out, linked-providers, and connected-apps assertions/selectors for the new structure, preserving every existing assertion — reviewed all three sections. Sign-out (`'Cerrar Sesión'`/`profile.signOut` key, `role="status"` toast, `TEST_USER_DISPLAY_NAME`) needs **no change**: `profile.signOut` is an existing i18n key reused verbatim by both current `Profile.tsx` and `ProfileDirectionB.tsx` (the T028 build reference), so the rendered Spanish text is stable across the rebuild. Connected-apps (`'Asistentes de IA conectados'`) also needs **no change**: sourced from `mcpConnector.connectedApps.title`, an established key `ProfileDirectionB.tsx` reuses verbatim too. Linked-providers: dropped the `'Métodos de Inicio de Sesión'` heading assertion (hardcoded Spanish, no i18n key backing it today — T029 is expected to introduce one, whose copy can't be predicted) in favor of asserting "Google"/"GitHub" are visible (provider names are rendered literally, not translated, in every direction inventoried for this feature — durable). Kept `'Vinculado y activo'` and the `'Vincular'` button-name assertion as-is (genuinely correct against today's implementation, and this spec's job — real backend, real session — differs from the now-thorough T024 unit-level state-matrix coverage); flagged both with an explicit comment that they're the most likely lines to need another update once T029 lands its own (currently nonexistent) i18n copy for this card, since — unlike `profile.signOut` — there's no pre-existing key to carry forward here. Verified against the real app: `firebase emulators:exec --only auth,firestore "npx playwright test --project=chromium profile.spec.ts"` — **9/9 passed**.

### Implementation for User Story 3

- [X] T028 [US3] Rebuild the sign-out control in `retro-rocket/src/pages/Profile.tsx` per the selected direction, preserving error feedback on failure (FR-004) — moved Sign Out out of the page header into a new "Access & Security" bordered section (`profile.accessSecurity.*`, new keys) rendered as a proper Settings-row (leading `LogOut` icon, `profile.signOut` title + description, trailing button) matching `ProfileDirectionB.tsx`'s `AccessSecuritySection` reference; added local `isSigningOut` state so the button shows `Button`'s existing `loading` affordance while `signOut()` is in flight. Error feedback unchanged: `UserContext.signOut()` already fires `toast.error()` on failure (research.md §5 — no production change needed there); the local `catch` only clears the loading state and logs, it doesn't duplicate the visible error. Also gated the page-header and Access-&-Security-column `motion.div` wrappers (previously ungated) with `useReducedMotion()`, reusing the T017-established `ENTRANCE_EASE`/0.24s decision. `e2e/profile.spec.ts`'s `'Cerrar Sesión'` button-name assertions needed no change (`profile.signOut` key reused verbatim).
- [X] T029 [US3] Rebuild `retro-rocket/src/features/auth/components/LinkedProvidersCard.tsx` per the selected direction, preserving the link flow and not-yet-available indication (FR-005) — rebuilt as a uniform Settings-row list (leading icon, primary/secondary text, trailing action) per `ProfileDirectionB.tsx`'s `ProviderRow`; **closes the real T024 gap**: Apple now always renders as its own row with `linkedProviders.statusNotAvailable` ("Not available yet"/"No disponible todavía") as a persistently visible caption plus a native-`disabled` "Vincular"/"Link" button with `aria-describedby` pointing at that caption (same FR-007 pattern as the account-action placeholders) — previously Apple was entirely absent from the rendered UI. All copy moved off hardcoded Spanish onto i18next (new `linkedProviders.*` namespace in both locales) — closes the FR-011 gap research.md/data-model.md flagged. Removed the decorative "Security Notice" box (not part of Direction B's row vocabulary, no FR requirement backed it). `LinkedProvidersCard.test.tsx`: 6/6 passed including the previously-red Apple test. `e2e/profile.spec.ts` updated: `'Vinculado y activo'` → `'Vinculado'` (`linkedProviders.statusLinked`), added an Apple-visible assertion, and scoped the `'Vincular'` button assertion to GitHub's row specifically (Apple's disabled row reuses the same "Vincular" label per `ProfileDirectionB.tsx`'s own `ProviderRow`, so an unscoped query now strict-mode-matches two buttons).
- [X] T030 [US3] Rebuild `retro-rocket/src/features/auth/components/ConnectedAppsCard.tsx` per the selected direction, preserving the list/revoke/loading/error behavior (FR-006) — restructured to a semantic `<ul>/<li>` Settings-row list (icon slot, name+origin caption, connected/last-used dates, trailing Revoke button) matching `LinkedProvidersCard`'s new vocabulary and `ProfileDirectionB.tsx`'s `ConnectedAppRow`; all list/revoke/loading/error/empty behavior unchanged (all 10 pre-existing `ConnectedAppsCard.test.tsx` assertions still pass, no red state — this file's behavior already satisfied FR-006, only structure changed). Motion (see T032) is now reduced-motion-gated, which it wasn't before this task.
- [X] T031 [US3] Rebuild the "Exportar mis datos"/"Eliminar cuenta" account-action placeholders in `retro-rocket/src/pages/Profile.tsx` per the selected direction, fixing the FR-007 accessibility gap: keep the native `disabled` attribute, add a persistently visible "not yet available" label, and associate it via `aria-describedby` per `research.md` §2 and `data-model.md`'s `Account Action Placeholder` — added a local `ActionPlaceholderRow` (modeled on `ProfileDirectionB.tsx`'s reference component) rendering both placeholders as `<li>` rows inside a bordered "Account Actions" `<section>`: each pairs a persistently visible `<span id=...>{unavailableLabel}</span>` (new `profile.accountActions.notAvailableLabel` key, both locales) with `<Button disabled aria-describedby={that id}>`. The button's own accessible name is now the real action (`title`, e.g. "Export Data"/"Delete Account") instead of the old "Coming soon"/"Delete" `.button` copy, which previously *was* the button's only accessible name and gave AT no separate description — closing the exact gap `Profile.test.tsx` (T026) found. Removed the now-unused `profile.accountActions.{exportData,deleteAccount}.button` keys from both locales. `Profile.test.tsx`: 9/9 passed, including the previously-red `aria-describedby` test (exactly 2 disabled buttons, each resolving to visible non-empty text).
- [X] T032 [US3] Using the `animate` skill, implement the connected-app revoke-in-progress→removed transition and any provider-linking-in-progress indicator motion, gated by the existing `useReducedMotion` hook (FR-013) — ran the `animate` skill for both decisions. **Connected-app revoke→removed** (`ConnectedAppsCard.tsx`): gate = occasional/deliberate (a security-relevant, infrequent action, not routine), purpose = state indication + preventing a jarring teleport; tool = framer-motion `AnimatePresence`/`motion.li` (already required for the exit to play at all); properties = `opacity`+`x` only; reused the page's established quiet curve/duration (`cubic-bezier(0.23,1,0.32,1)`, 0.18s, matching `ProfileDirectionB.tsx`'s own `ConnectedAppRow` reference) for both enter and exit; fully skipped (`duration:0`, no x offset) under `useReducedMotion()` — the actual gap this closes, since the pre-rebuild version had this exact motion.div entirely ungated. **Provider-linking-in-progress indicator** (`LinkedProvidersCard.tsx`): gate = rare/deliberate; purpose = feedback (confirming the click registered before `startLinkProvider()`'s synchronous `window.location.assign` redirect actually unloads the page, closing the perceptible no-feedback gap research.md §3 flagged) — concluded the correct, cheapest-tool-that-works answer is *no new motion at all*: wired a local `linkingProviderId` state to the existing `Button` `loading`/`disabled` affordance (an already-adopted, already-reduced-motion-agnostic-at-the-CSS-level `animate-spin` spinner) rather than hand-rolling bespoke framer-motion, per the skill's "cheapest tool that works" step. Also dropped per-row entrance motion from `LinkedProvidersCard`'s three static rows entirely (previously ungated `motion.div x:-10→0` on every row): re-ran the gate on that pre-existing motion and it fails step 2 ("what's the purpose?" — no state change, no jarring teleport to bridge on three rows rendered once per page load) — `ProfileDirectionB.tsx`'s own `ProviderRow` reference doesn't animate this list either, so removing it (rather than gating it) is the correct call per the skill's own philosophy, not a scope-reduction.

**Checkpoint**: User Stories 1-3 all work independently — sign-out,
provider linking, connected-app management, and the corrected disabled
placeholders all behave correctly.

---

## Phase 6: User Story 4 - Consistent, Accessible Experience for Every Visitor (Priority: P3)

**Goal**: The redesigned Mi Perfil remains legible, operable, and coherent
across themes, locales, motion preferences, and viewport sizes, for every
state introduced or touched by Stories 1-3.

**Independent Test**: Load Mi Perfil on narrow mobile and ultra-wide
desktop viewports, in both themes, in both languages, and with reduced
motion enabled — every capability from Stories 1-3 remains available and
legible; the landing page's first-time-setup rendering also remains
correct.

### Tests for User Story 4 ⚠️

- [X] T033 [P] [US4] Extend `retro-rocket/e2e/accessibility.spec.ts` to axe-scan `/perfil` across every `Profile View State` variant (loaded, loading, error) and the display-name `saving`/`save-error` states, in both themes, per `contracts/accessibility-interaction-contract.md` — added scans for loading + saving + save-error (loaded/error pre-existed feature 018); **found and fixed a real bug during verification**: the "loading" scan expected Profile.tsx's own inner `Cargando...` text, but `AuthWrapper.tsx` gates rendering Profile's children on one combined `loading` flag that only clears once BOTH session-establishment AND the profile fetch resolve together (`UserContext.tsx`'s bootstrap effect sets `userData` + `coreState.loading:false` in the same synchronous block) — so on a real navigation, AuthWrapper's own "Verificando autenticación..." is what's actually visible during this window; Profile.tsx's own loading branch is real (independently covered by `Profile.test.tsx`, which mocks context past AuthWrapper) but is currently unreachable end-to-end. Fixed the scan to assert what a real visitor actually sees. All 6 scans (3 states × 2 themes) passing.
- [X] T034 [P] [US4] Add an accessibility assertion confirming the disabled "Exportar mis datos"/"Eliminar cuenta" placeholders are announced as unavailable — not merely visually muted — in 100% of automated runs (SC-007) — dedicated test (not folded into the loaded-state scan, since axe's `aria-describedby` rule only confirms the id resolves to *some* element, not that it's visible/non-empty): asserts both buttons are `disabled` and their `aria-describedby` resolves to visible, non-empty text. Passing.
- [X] T035 [P] [US4] Add/extend responsive-viewport checks (narrow mobile, ultra-wide desktop) for the rebuilt Mi Perfil per `quickstart.md`, in `retro-rocket/e2e/profile.spec.ts` or a dedicated responsive spec (FR-014) — new `retro-rocket/e2e/profile-responsive.spec.ts`, mirroring `board-responsive.spec.ts`'s `expectNoHorizontalOverflow` pattern. **Found and fixed two real bugs during verification, not just test-authoring**: (1) a genuine structural mobile-overflow defect — `Profile.tsx`'s `<div className="grid lg:grid-cols-3 gap-8">` had no base `grid-cols-1`, so below `lg` the implicit single grid column sized to its widest child's max-content width instead of `minmax(0,1fr)`, letting untranslated content force the whole page ~27-71px wider than the 375px viewport; fixed by adding the base `grid-cols-1` class, verified via a throwaway diagnostic script (0 overflow offenders after the fix, deleted after use) and 0/5 reproduction on repeat; (2) a locator bug in this new spec itself — the Header's user-menu span renders the same display name (`hidden md:block`, hidden below `md`), so an unscoped `getByText(...).first()` on the narrow viewport matched that *hidden* span instead of Mi Perfil's own visible one; fixed by scoping all assertions to a new `data-testid="profile-content"` on `Profile.tsx`'s root. Also found and fixed a small (~1/5) motion-timing flake: the overflow check could catch a mid-entrance-transition frame; added a 400ms settle wait (precedent: `landing-capture.ts`). 16/16 passing across 8 repeats after all three fixes.
- [X] T036 [P] [US4] Add a keyboard-only operability test covering edit/save display name, sign out, link a provider, and revoke a connected app — Tab to reach, Enter/Space to activate, no mouse involved — per `contracts/accessibility-interaction-contract.md` — 4 separate tests (sign-out and provider-linking each end in a real navigation, so splitting avoids one test's teardown blocking the next); revoke-a-connected-app case seeded via `registerAndConnectMcpClient`'s real consent-screen click (setup step, not the interaction under test — same precedent as this file's existing board keyboard tests); provider-linking verified up to intercepting the real `/api/auth/link/github` redirect (OAuth consent screen itself out of scope, as the task allows). All 4 passing.

### Implementation for User Story 4

- [X] T037 [US4] Verify contrast for any tokens added in T011 across every `Profile View State` variant in both themes, per `contracts/accessibility-interaction-contract.md`; fix any failure found — **no fix needed, verified passing**: `npx vitest run src/test/lib/theme/contrast.tokens.test.ts --config vitest.config.ts` → 50/50 passed in both themes; `git diff src/lib/theme/tokens.ts` confirms `CONTRAST_PAIRINGS` has received no changes since T011 (only the 4 pairings T011 added: `success-fg`/`error-fg` × `surface`/`surface-raised`). Traced each pairing to a real rendered instance in the now-fully-rebuilt page: `error-fg`/`success-fg` on `surface-raised` is exercised by `UserProfileForm.tsx`'s save error/success inline text (its form container has no own `bg-*`, inheriting the card's `bg-surface-raised`), by `LinkedProvidersCard.tsx`/`ConnectedAppsCard.tsx`'s error/linked-status text (both render inside the shared `Card` component's default `bg-surface-raised` variant), and by `Profile.tsx`'s danger-toned `ActionPlaceholderRow` title/description text (inside the `bg-surface-raised` Account Actions section). `error-fg`/`success-fg` on plain `surface` is not currently hit by any rendered instance in these 4 files (only `error-fg`/`success-fg` on `surface-raised` is) — the `surface` pairings remain a valid defensive entry (T011's own reasoning) but aren't exercised by today's actual DOM; no gap to fix since nothing renders that combination incorrectly.
- [X] T038 [US4] Run the `review-animations` skill (Constitution Principle IX) as a critique pass over all motion introduced in T017/T022/T032, documenting findings and applying any fixes — ran the skill against the actual current implementation (not the task summaries) of `Profile.tsx`'s identity-panel entrance (T017), `UserProfileForm.tsx`'s save feedback (T022), and `ConnectedAppsCard.tsx`/`LinkedProvidersCard.tsx`'s revoke transition + linking indicator (T032). **Verdict: Approve for all three**, each independently satisfying the ten non-negotiable standards (justified purpose, frequency-appropriate, strong custom ease-out, sub-300ms, GPU-only `opacity`/transform properties, transitions not keyframes, reduced-motion gated, no color-only meaning). T032's choice to reuse `Button`'s existing `loading` affordance instead of adding new motion for the linking indicator, and its removal of the previously-ungated, purposeless per-row entrance motion from `LinkedProvidersCard`'s static rows, were both called out as exemplary applications of the remedial hierarchy's "delete the animation" step. **One real bug found and fixed** during the review, adjacent to T022's file though not part of T022's own decision: `UserProfileForm.tsx`'s outer card `motion.div` (line ~120-125) had `transition={reducedMotion ? {duration:0} : undefined}` — the `undefined` branch let Framer Motion fall back to its own default (a tween for `opacity` but an untuned spring for the transform `y`), producing a faint bounce inconsistent with this file's own `EASE_OUT` curve used two nodes below it. Also found this same wrapper double-animates on Mi Perfil: it's nested inside `Profile.tsx`'s own "Edit Profile Form" column entrance (`x:20→0`, 0.24s), so both playing at once compounded into an uncontrolled diagonal slide; Landing's first-time-setup call site has no such parent wrapper so this component's own entrance must stay there. Fixed by giving the wrapper an explicit, consistent transition and gating its `initial` state on `!isFirstTime` (in addition to `reducedMotion`) so it's skipped on Mi Perfil (where the parent already handles entrance) but still plays on Landing. Verified: `npx vitest run src/test/features/auth/UserProfileForm.test.tsx`: 21/21 passed after the fix; `type-check`/`lint` clean.
- [X] T039 [US4] Verify i18next key coverage: no hardcoded strings were introduced across T014-T032, and every new/renamed `profile.*`/`auth.userProfileForm.*` key exists in both `retro-rocket/src/locales/en.json` and `retro-rocket/src/locales/es.json` (FR-011) — verified systematically, no gaps found, no fix needed. (a) Grepped `Profile.tsx`/`UserProfileForm.tsx`/`LinkedProvidersCard.tsx`/`ConnectedAppsCard.tsx` for JSX text nodes and `placeholder`/`title`/`aria-label`/`alt` attribute literals outside `t(...)` calls: zero hits. The only literal strings left in these files are the provider brand names ("Google"/"GitHub"/"Apple" in `getProviderName()` and `PROVIDER_DEFS`) — an intentional, already-documented (T024/T027) design decision to render proper nouns literally rather than translate them, consistent across every direction explored for this feature, not a gap. Re-grepped for hardcoded Spanish specifically (vincul-, cerrar sesión, guardar, nombre a mostrar, correo, cuenta, eliminar, exportar, próximamente, cargando): only hits were in code comments, none in rendered JSX — **confirms `LinkedProvidersCard.tsx` (previously flagged as hardcoding its entire copy in Spanish outside i18next) genuinely has zero hardcoded strings now**, not just a claimed fix. (b) Extracted every literal `t('...')` key referenced across the 4 files (56 keys) plus the 4 dynamically-constructed `mcpConnector.connectedApps.origin${Origin}` keys and diffed them against both `en.json`/`es.json`: all present in both, zero missing. Separately flattened both locale files' `profile.*`/`auth.userProfileForm.*`/`linkedProviders.*` namespaces (17/17/6 keys respectively) and diffed the two key sets against each other directly: zero drift either direction — no stray keys in one locale missing from the other, and no dangling now-unused keys left behind (e.g. T031's removed `profile.accountActions.{exportData,deleteAccount}.button` keys are confirmed actually gone from both files, not just unreferenced).

**Checkpoint**: All four user stories are independently functional and
consistently accessible.

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: Final verification against the feature's contracts and
success criteria, across all four user stories together.

- [X] T040 [P] Reconcile any remaining copy differences introduced by the redesign in `retro-rocket/src/locales/en.json` and `retro-rocket/src/locales/es.json`, keeping both files' `profile.*`/`auth.userProfileForm.*` keys in lockstep — final programmatic diff confirms exact lockstep: `profile.*` (17/17), `auth.userProfileForm.*` (17/17), `linkedProviders.*` (6/6) — zero drift either direction; no reconciliation needed
- [X] T041 Run the full verification procedure in `specs/050-profile-redesign/contracts/functional-parity-contract.md` (final run against the T001 baseline for every FR-002-FR-011 row, including the FR-007 `[CORRECTED]` row) and record the result — all 11 rows PASS; recorded in the contract file's new "Result" section (full-suite numbers: type-check 0 errors, lint clean, test:coverage 186 files/2515 tests, 78.81/83.24/77.04/78.81%, e2e 88/88)
- [X] T042 Run the full checklist in `specs/050-profile-redesign/contracts/accessibility-interaction-contract.md` end-to-end and record the result — all 12 checkboxes verified and checked `[x]`, each with a note on how it was verified; recorded in the contract file itself
- [X] T043 [P] Produce `specs/050-profile-redesign/design-review.md` documenting a structured Apple HIG design review (clarity, deference, depth) of the shipped direction, closing with zero unresolved high-priority findings (SC-004) — analogous to features 028/029/031's `design-audit.md`/`design-review.md` — done: reviews the actual shipped code (not aspirational description); 2 findings both resolved-in-shipped-work or explicitly scoped as non-blocking low-priority (duplicate read-only email between Identity panel and Edit Profile Form — intentional, no functional/accessibility consequence); outcome: zero unresolved high-priority findings
- [X] T044 Run `specs/050-profile-redesign/quickstart.md` end-to-end (all four sections) and record the result in a "Validation run" section appended to `quickstart.md` — done; all 4 sections verified for real against the shipped implementation, numbers match T041/T045
- [X] T045 Final regression sweep: `npm run type-check`, `npm run lint`, `npm run test:coverage`, and `npm run e2e -- profile.spec.ts authentication.spec.ts accessibility.spec.ts` — confirm zero regressions against the T001 baseline and unchanged coverage thresholds — **zero regressions, coverage improved**: type-check 0 errors (baseline: 0); lint 0 errors/warnings (baseline: 0); test:coverage 186 files/2515 tests passed, 2/3 skipped (baseline: 185/2503, 2/3 skipped) — coverage 78.81% stmts / 83.24% branches / 77.04% funcs / 78.81% lines vs. baseline's 77.45/83.42/76.9/77.45% (stmts/funcs/lines improved, branches essentially flat, all still comfortably above the 50/78/64/50 floor); e2e `profile.spec.ts authentication.spec.ts accessibility.spec.ts` via the direct `firebase emulators:exec`/`playwright test` invocation (npm script argument-passing quirk noted in T001) — **88/88 passed** (baseline: 76/77, 1 pre-existing flake) — the flake did not reproduce in this final run. Feature 050-profile-redesign is complete: all 45 tasks done.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — can start immediately
- **Foundational (Phase 2)**: Depends on Setup completion — BLOCKS all user stories (the selected direction and the product-owner decision artifact are prerequisites for every story's rebuild)
- **User Stories (Phase 3-6)**: All depend on Foundational phase completion
  - US1 (P1) has no dependency on US2-US4
  - US2 (P2) touches only `UserProfileForm.tsx` — file-disjoint from US1/US3 — independently testable with no ordering constraint
  - US3 (P2) touches `retro-rocket/src/pages/Profile.tsx`, the same file US1 rebuilds — implement after US1 to avoid rework, though its tests are independent
  - US4 (P3) is a cross-cutting verification pass over Stories 1-3's output — implement last
- **Polish (Phase 7)**: Depends on all four user stories being complete

### Within Each User Story

- Tests MUST be written and FAIL (where marked) before the corresponding implementation task
- `Profile.tsx` is touched by both US1 (T014-T016) and US3 (T028, T031) — sequence these within a story or merge carefully if split across people
- Story complete before moving to the next priority

### Parallel Opportunities

- T003, T004, T005 (the three prototype directions) run in parallel — different files, no dependencies
- T007 runs in parallel with the prototype drafts
- All Setup tasks marked [P] can run in parallel
- Within each user story, all test tasks marked [P] run in parallel; most implementation tasks touch shared files and are sequential within that story
- US2's tasks (T018-T023) can run fully in parallel with US1/US3's, since `UserProfileForm.tsx` shares no file with `Profile.tsx`, `LinkedProvidersCard.tsx`, or `ConnectedAppsCard.tsx`

---

## Parallel Example: Foundational Phase

```bash
# Launch the three visual-direction drafts together:
Task: "Draft Visual Direction A in src/pages/__prototypes__/ProfileDirectionA.tsx"
Task: "Draft Visual Direction B in src/pages/__prototypes__/ProfileDirectionB.tsx"
Task: "Draft Visual Direction C in src/pages/__prototypes__/ProfileDirectionC.tsx"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup
2. Complete Phase 2: Foundational (CRITICAL — blocks all stories)
3. Complete Phase 3: User Story 1
4. **STOP and VALIDATE**: Test User Story 1 independently (view profile,
   both loading/error states)
5. Deploy/demo if ready

### Incremental Delivery

1. Complete Setup + Foundational → direction selected, decision artifact delivered
2. Add User Story 1 → test independently → deploy/demo (MVP!)
3. Add User Story 2 → test independently → deploy/demo
4. Add User Story 3 → test independently → deploy/demo
5. Add User Story 4 → cross-cutting verification → deploy/demo
6. Complete Polish phase → final contract/quickstart sign-off

### Parallel Team Strategy

With multiple developers:

1. Team completes Setup + Foundational together (direction selection needs
   a single product-owner review checkpoint, T009)
2. Once Foundational is done:
   - Developer A: User Story 1 (owns `Profile.tsx`'s identity header/date/loading/error states)
   - Developer B: User Story 2 (owns `UserProfileForm.tsx` — no file overlap with Developer A)
   - Developer C: waits for User Story 1's `Profile.tsx` work to land, then starts User Story 3's account-access and placeholder work on the same file to avoid merge conflicts
3. User Story 4 starts once Stories 1-3 are merged

---

## Notes

- [P] tasks = different files, no dependencies
- [Story] label maps task to specific user story for traceability
- `Profile.tsx` is touched by both US1 (T014-T016) and US3 (T028, T031) —
  sequence these within a story or merge carefully if split across people
- Verify tests fail before implementing (especially T026, the FR-007
  regression test, which must fail against the *current* missing
  `aria-describedby` association)
- Commit after each task or logical group
- Stop at any checkpoint to validate story independently
- Avoid: vague tasks, same-file conflicts without sequencing, cross-story
  dependencies that break independence
