---

description: "Task list template for feature implementation"
---

# Tasks: Retrospective Board Redesign (Apple HIG-Inspired)

**Input**: Design documents from `/specs/033-retro-board-redesign/`

**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/](./contracts/), [quickstart.md](./quickstart.md)

**Tests**: Tests are included for behavior at real regression risk —
drag-and-drop, grouping, card CRUD/vote/like/react, facilitator controls,
export, and the new ARIA-tabs/accessibility-coverage corrections — per
constitution Principle I (TDD, NON-NEGOTIABLE). Pure visual/motion
restyling with no pre-existing behavior to protect follows the precedent
features 028/029/031 established (no new test required for cosmetic-only
change); every such task below says so explicitly.

**Organization**: Tasks are grouped by user story (spec.md's US1-US5) to
enable independent implementation and testing of each. A Foundational phase
precedes all of them because FR-018 requires exploring and selecting one of
2-3 visual directions, and a shared menu-overlay hook (consolidating the
duplicated positioning/dismissal logic across the board's four menus) is a
dependency every story's menu-bearing components need before their
story-specific rebuild can proceed.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1, US2, US3, US4, US5)
- File paths are relative to `retro-rocket/` unless otherwise noted

## Path Conventions

Single existing React SPA frontend project — all paths are under
`retro-rocket/src/` (or `retro-rocket/e2e/` for Playwright specs), per
`plan.md`'s Project Structure. No backend/API paths are touched.

---

## Phase 1: Setup

**Purpose**: Establish a regression baseline, clear out dead code in the
area being rebuilt, and prepare a place to build the prototyped visual
directions without touching the shipped board yet.

- [X] T001 Establish baseline: run `npm run type-check`, `npm run lint`, `npm run test:coverage`, and `npm run e2e -- retrospective-board.spec.ts board-creation.spec.ts board-join.spec.ts export.spec.ts facilitator-countdown.spec.ts concurrent-board-session.spec.ts concurrent-board-network.spec.ts accessibility.spec.ts` from `retro-rocket/` and record the passing baseline (coverage numbers, test counts) to compare against after implementation (FR-019). This full-suite pass is also the pre-redesign baseline `contracts/functional-parity-contract.md`'s verification procedure checks every capability row against. — **Baseline (2026-08-09)**: type-check 0 errors; lint 0 errors/warnings; `test:coverage` 165 files/2448 tests passed (2 skipped files/3 skipped tests), thresholds met (66.83% statements / 83.02% branches / 72.44% functions / 66.83% lines, all above 50/78/64/50); board e2e 67/68 passed, 1 pre-existing flaky failure (`concurrent-board-session.spec.ts`'s 10-participant forced-reconnect load test, unrelated to the board redesign — matches the same flake class documented in feature 031's baseline)
- [X] T002 [P] Remove the dead export components with zero non-test references — `retro-rocket/src/features/boards/export/components/ExportButtonGroup.tsx`, `UnifiedExporter.tsx`, and `DocxExporter.tsx` (confirmed unused; `ImprovedExportPopover.tsx` + `useUnifiedExport` is the sole live export path) — along with their now-orphaned tests (e.g. `ExportButtonGroup.test.tsx`), per Constitution V (Simplicity) and the precedent set by feature 010's FR-014 dead-component removal. — **Done**: re-confirmed zero non-test references before deleting; all four files removed; type-check/lint clean; remaining 9 export test files (71 tests) pass unchanged.
- [X] T003 [P] Add a dev-only prototype route scaffold in `retro-rocket/src/App.tsx` (e.g. gated behind `import.meta.env.DEV`) that will mount the 2-3 candidate `RetrospectiveBoard` variants side by side for review, per `contracts/visual-direction-review-contract.md`. — **Done**: `RetrospectiveBoardDirectionsScaffold.tsx` (tab switcher, dev-only route `/dev/retro-directions/:id`) + a shared `usePrototypeBoardData` hook (real `useRetrospectiveRealtimeSync`/`useOptimizedCards`/`useCardGroups`/`useActionItems` wiring, called once and passed down so all 3 candidates share one realtime subscription); wired into `App.tsx` behind `import.meta.env.DEV`.

**Checkpoint**: Baseline recorded, dead export code removed, prototype scaffold ready.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Explore and select the one Visual Direction (FR-018) all user
stories will be built against, and build the shared menu-overlay hook every
story's menu components depend on (consolidating the duplicated positioning/
dismissal logic in `RetrospectiveTopbar.tsx`, `FacilitatorMenu.tsx`,
`CardMenu.tsx`, and `ColumnHeaderMenu.tsx`, per `research.md` §3).

**⚠️ CRITICAL**: No user-story section work can begin until the product
owner has selected a direction (T010) and the shared overlay hook (T013) is
in place.

- [X] T004 [P] Using the `apple-design`/`emil-design-eng` skills (the constitution-mandated `prototype` skill is not installed in this environment; per the precedent established in features 029/031, `apple-design`/`emil-design-eng` are substituted for building real interactive candidates), draft Visual Direction A as a real, working board variant in `retro-rocket/src/pages/__prototypes__/RetrospectiveBoardDirectionA.tsx`, wired to a seeded board's real data, satisfying every item in `contracts/visual-direction-review-contract.md`'s "Required before review" checklist. — **Done**: "Focused Canvas" (deference-forward) — translucent floating toolbar, cards float on the page background with no border at rest (soft ring on hover/focus only), quiet/low-contrast actions that firm up on interaction, generous whitespace/line-height over density; real add/vote/like/react/drag-reorder against live board data via `usePrototypeBoardData`; embeds the real `FacilitatorMenu`/`ImprovedExportPopover`/`CountdownTimer`/`ResponsiveParticipantDisplay` for full functional completeness. Type-check clean.
- [X] T005 [P] Using the same substituted skills, draft Visual Direction B as a real, working board variant in `retro-rocket/src/pages/__prototypes__/RetrospectiveBoardDirectionB.tsx`, genuinely distinct from Direction A per `data-model.md`'s `distinguishingChoices` field (including its own `menuPresentationPattern` per `research.md` §7), satisfying the same review-contract checklist. — **Done**: "Structured Clarity" (clarity-forward) — opaque tinted column header bars, full visible card borders at rest (not hover-revealed), always-visible action row and card-menu trigger (no opacity choreography), solid non-translucent topbar/dropdown, denser type/spacing scale. Same real data wiring as Direction A. Type-check clean.
- [X] T006 [P] Using the same substituted skills, draft Visual Direction C as a real, working board variant in `retro-rocket/src/pages/__prototypes__/RetrospectiveBoardDirectionC.tsx`, genuinely distinct from Directions A and B, satisfying the same review-contract checklist. — **Done**: "Layered Depth" (materials/depth-forward) — floating glass toolbar detached from the viewport edge, translucent tinted-gradient column panels, elevated glass cards whose shadow intensifies while dragging (tactile lift, via a new `isDragging` param added to the shared `PrototypeDragColumn`'s render callback). Same real data wiring as A/B. Fixed a genuine `jsx-a11y/interactive-supports-focus` finding (the `role="menu"` panel needed `tabIndex={-1}`) across all three direction files. Type-check and lint both clean.
- [X] T007 [P] Author a board-seeding helper (e.g. `retro-rocket/e2e/fixtures/seedBoardCards.ts`, extending the existing `e2e/fixtures/board.ts` pattern) that populates a board with 30+ cards in at least one column, several groups, action items, facilitator notes, and up to 15 simulated concurrent participants against the real backend, for use in direction review and later scale verification (supports FR-018 review realism, SC-001, SC-007). — **Done**: `seedBoardCards.ts` — `seedBoardCards`/`seedBoardGroups`/`seedActionItems`/`seedFacilitatorNotes` (direct REST calls, batched, mirroring `seedBoards.ts`'s pattern) plus `seedParticipants`/`closeParticipants` (one isolated `BrowserContext` per simulated participant, mirroring `concurrent-board-session.spec.ts`'s established pattern, for up to 15 concurrent identities). Isolated `tsc --noEmit` compile clean (e2e/ is outside the main `src`-scoped type-check/lint configs, consistent with the existing `seedBoards.ts`).
- [X] T008 Wire the three prototypes into the dev-only route scaffold from T003 in `retro-rocket/src/App.tsx`, using the T007 seeding helper to populate a realistically dense board, so they are viewable side by side in both themes and at mobile/desktop widths. — **Done**: verified via a throwaway Playwright script (seeded 32/6/6 cards across 3 columns + 3 groups + 3 action items + 2 facilitator notes via `seedBoardCards.ts`, navigated `/dev/retro-directions/:id`, switched all 3 tabs, screenshotted, asserted zero console errors and all 3 column headings/cards genuinely present in the DOM — not just visually plausible; deleted after use, matching feature 031's precedent). All three directions render correctly against real dense data. Every component uses the app's existing semantic tokens (`bg-surface`, `text-primary`, etc., dark-mode-aware) and the `grid-cols-1 lg:grid-cols-3` responsive pattern already established elsewhere, so both themes and mobile/desktop widths are supported by construction; per-theme/per-viewport screenshot comparison is the product owner's own review step at T010, not re-verified separately here.
- [X] T009 [P] Record all three candidates in `specs/033-retro-board-redesign/data-model.md`'s `Visual Direction` table (`concept`, `distinguishingChoices`, `menuPresentationPattern`, `newDependencies`). — **Done**: all three recorded with `status = explored`; `status`/`reviewedBy`/`rejectionReason` finalization is deliberately left pending T010's human review.
- [X] T010 Product-owner review checkpoint: present the three candidates per `contracts/visual-direction-review-contract.md`'s review procedure; record the selected direction and the two `rejected` ones (with `rejectionReason`) in `specs/033-retro-board-redesign/data-model.md`. Because the constitution-mandated `prototype` skill isn't installed in this environment (`plan.md`'s Constitution Check row IX, `research.md` §5), this checkpoint MUST also have the product owner explicitly acknowledge the `apple-design`/`emil-design-eng` substitution used to build T004-T006, alongside approving the chosen direction. — **Resolved 2026-08-09**: presented via a published comparison artifact (light/dark screenshots against a seeded board); product owner (Fernando Ortiz) selected **Direction C — Layered Depth**. A and B rejected with reasons recorded in `data-model.md`.
- [X] T011 Delete the two non-selected prototype files from `retro-rocket/src/pages/__prototypes__/` and remove the dev-only route scaffold from `retro-rocket/src/App.tsx` added in T003/T008. — **Done**: `RetrospectiveBoardDirectionA.tsx`, `RetrospectiveBoardDirectionB.tsx`, and the scaffold `RetrospectiveBoardDirectionsScaffold.tsx` deleted; route/lazy-import removed from `App.tsx`. `RetrospectiveBoardDirectionC.tsx` kept unrouted as a build reference for T021+ (same precedent as feature 031's T010), deleted during Polish once the real rebuild supersedes it. Type-check/lint clean.
- [X] T012 [P] If the selected direction requires new design tokens (`data-model.md`'s `Design Token Extension`), add them to `retro-rocket/src/lib/theme/tokens.ts` and add/extend their `CONTRAST_PAIRINGS` entries in `retro-rocket/src/lib/theme/contrast.ts`, verified by `contrast.tokens.test.ts` in both themes — skip explicitly (record "no new tokens needed") if the selected direction uses only existing tokens. — **Skipped**: Direction C (Layered Depth) uses only existing semantic tokens (`surface`, `surface-raised`, `text-*`, `border-*`, `action`, `focus`, `success`/`error`/`info`/`warning`-bg/fg) throughout — confirmed via grep, zero hardcoded palette classes or hex/rgb literals. No new tokens needed.
- [X] T013 [P] Write failing unit tests then implement a shared menu-overlay hook (e.g. `retro-rocket/src/features/boards/retrospective/hooks/useBoardMenuOverlay.ts`) providing anchored positioning (wrapping the existing `@floating-ui/react` foundation), outside-click dismissal, Escape-key dismissal, and focus-return behavior, in `retro-rocket/src/test/features/boards/retrospective/useBoardMenuOverlay.test.ts` — consolidates the duplicated logic currently hand-rolled in `RetrospectiveTopbar.tsx`, `FacilitatorMenu.tsx`, `CardMenu.tsx`, and `ColumnHeaderMenu.tsx` (`research.md` §3, Constitution Principle II). — **Done**: 7/7 tests passing (written first, confirmed failing against the not-yet-existing hook, then implemented to pass); mirrors `useEmojiPicker.ts`'s established Floating UI pattern (offset/flip/shift/size/autoUpdate, `useClick`/`useDismiss`/`useRole`/`useInteractions`), generalized with configurable `placement`/`role`/controlled-open support and exposing `context` for `FloatingFocusManager`-based focus-trap/return, matching `ReactionPicker.tsx`'s consumption pattern. Type-check/lint clean.

**Checkpoint**: Selected direction (and the skill-substitution acknowledgment)
recorded, shared menu-overlay hook in place and unit-tested — user story
implementation can now begin.

---

## Phase 3: User Story 1 - Contribute and React During a Live Session (Priority: P1) 🎯 MVP

**Goal**: A participant can add, edit, delete, vote on, like, and react to
cards across the board's columns, and see live presence/typing indicators,
presented through the selected visual direction — including the column-grid
container itself (FR-006).

**Independent Test**: As a participant in a board with other active
participants, add a card, edit it, vote on another participant's card, like
a card, add/remove an emoji reaction via the reaction picker, delete a card,
and observe another participant's presence and typing indicator — all in
real time, at the validated scale (30+ cards/column, up to 15 participants).

### Tests for User Story 1 ⚠️

- [X] T014 [P] [US1] Update `retro-rocket/src/test/features/boards/retrospective/CardContent.test.tsx`, `CardHeader.test.tsx`, `CardFooter.test.tsx` to match the selected direction's markup, preserving content-wrap/URL-auto-link/line-break-preservation assertions (FR-002) — **Done**: added a regression-guard test proving CardFooter's edit/delete actions are never `opacity-0` (a real hover-only defect found and fixed in T021); CardContent/CardHeader needed no new assertions. 13/13 passing.
- [X] T015 [P] [US1] Update `retro-rocket/src/test/features/boards/retrospective/CardVoteControl.test.tsx`, `LikeButton.test.tsx` to match the new markup, preserving vote/like assertions (FR-003) — **Done**: no changes needed, see T022. 29/29 LikeButton + 2/2 CardVoteControl (colocated in `CardHeader.test.tsx`) passing.
- [X] T016 [P] [US1] Update `retro-rocket/src/test/features/boards/retrospective/ReactionPicker.test.tsx`, `EmojiReactions.test.tsx`, `ReactionBadge.test.tsx` to match the new markup, preserving anchored-positioning and add/remove-reaction assertions (FR-003) — **Done, no changes needed**: see T023. 25/25 passing (incl. `EmojiReactions.simple.test.tsx`).
- [X] T017 [P] [US1] Update `retro-rocket/src/test/features/boards/participants/ResponsiveParticipantDisplay.test.tsx`, `ParticipantPopover.test.tsx`, `CompactAvatarGroup.test.tsx`, `UserAvatar.test.tsx` to match the new markup (FR-008) — **Done**: updated `UserAvatar.test.tsx` (5 assertions) and `CompactAvatarGroup.test.tsx` (2 assertions) for the new semantic-token background and i18n-key rendering (see T027's two real defect fixes). 216/216 passing across all 8 participant test files.
- [X] T018 [P] [US1] Add an empty-column-state assertion to `retro-rocket/src/test/features/boards/clustering/GroupableColumn.test.tsx` asserting the empty state renders distinctly and invites the first contribution (edge case, `data-model.md`'s `Board State`) — **Done**: the existing "empty cards array" test only checked for no-crash; added a new test asserting the icon, `noCards` message, and `addFirstCard` CTA all genuinely render. 42/42 passing.
- [X] T019 [P] [US1] Update `retro-rocket/src/test/features/boards/retrospective/RetrospectiveBoard.test.tsx` and `RetrospectiveBoard-simple.test.tsx` to match the selected direction's markup, preserving column-count/grid-width sizing (`useBoardGridColumns`), the `showActionColumn` toggle, and responsive breakpoint-stacking assertions (FR-006, `data-model.md`'s `Board Layout State`) — **Done**: this file previously had zero assertions on the `board-grid` element's grid-sizing classes at all (the exact gap `/speckit-analyze` found as G1) — added two new tests asserting `lg:grid-cols-4`/`grid-cols-1` when the action column is shown and `lg:grid-cols-3` when hidden (spying on `uiPreferencesStore.getShowActionColumn`). 14/14 passing; `RetrospectiveBoard-simple.test.tsx` (12/12) left as a lighter duplicate, no unique coverage to add.
- [X] T020 [US1] Update `retro-rocket/e2e/retrospective-board.spec.ts` selectors/assertions for card add/edit/delete/vote/like/react and presence/typing indicators for the new structure — **Done, no changes needed**: ran the full spec against the real emulator-backed dev server after T021-T027's rebuild — 32/32 passing unmodified. The rebuild only touched styling classes and the two hover-only fixes (both additive — controls that were reachable stayed reachable), never testids/roles/accessible-names/text content the selectors depend on.

### Implementation for User Story 1

- [X] T021 [P] [US1] Rebuild `retro-rocket/src/features/boards/retrospective/components/CardHeader.tsx`, `CardContent.tsx`, `CardFooter.tsx` per the selected direction — **Done**: `CardHeader` simplified to match the reviewed prototype (dropped the metadata icon, quiet text-only author line); `CardContent` already matched the prototype exactly, unchanged; `CardFooter` fixed a real pre-existing defect — edit/delete actions were `opacity-0 group-hover:opacity-100` with no `focus-within` (hover-only, invisible to keyboard/touch users at rest) — now always visible, consistent with Direction C's already-visible vote/like/react row.
- [X] T022 [P] [US1] Rebuild `retro-rocket/src/features/boards/retrospective/components/CardVoteControl.tsx`, `LikeButton.tsx` per the selected direction — **Done, unchanged**: both were deliberately embedded as-is in the Direction C prototype during exploration (research.md's scoping: nested atomic controls inside an already-glass card stay opaque/simple for legibility rather than compounding `backdrop-blur` on small text) — what the product owner reviewed and approved already reflects this treatment; no further change needed.
- [X] T023 [US1] Rebuild `retro-rocket/src/features/boards/retrospective/components/ReactionPicker.tsx`, `ReactionBadge.tsx`, `EmojiReactions.tsx` per the selected direction, preserving `@floating-ui/react` anchored positioning and adopting the shared overlay hook from T013 where applicable — **Done, unchanged, T013 hook not adopted here (deliberate)**: these already embed as reviewed in the Direction C prototype. `useEmojiPicker.ts` is the hand-tuned hook `useBoardMenuOverlay` (T013) was itself modeled after — it already is the canonical proven pattern, not one of the four duplicated ad hoc implementations T013 exists to consolidate (options/facilitator/card/column-header menus). Migrating it onto the newer generalized hook now would be pure churn on working, tested code (Constitution V, Simplicity/YAGNI) with no behavior change to justify it.
- [X] T024 [P] [US1] Rebuild `retro-rocket/src/features/boards/retrospective/components/DraggableCard.tsx`, `SortableCard.tsx`, `SelectableCard.tsx` (card shells) per the selected direction — **Done**: `DraggableCard.tsx` — rounded-2xl surface, resting `shadow-md`/hover `shadow-lg`, dragging `shadow-2xl` (Direction C's tactile-lift shadow intensification); fixed a second real hover-only defect found while rebuilding — the convert-to-action `CardMenu` wrapper was `opacity-0 group-hover:opacity-100` with no `focus-within` (same class as CardFooter's, T021) — now always visible. Added a regression-guard test (mirroring T021's) proving no `opacity-0` ancestor. `SortableCard.tsx`/`SelectableCard.tsx` are thin wrappers with no visual treatment of their own — unchanged. 37/37 (DraggableCard) + 27/27 (DragDropColumn) passing; type-check/lint clean.
- [X] T025 [US1] Rebuild `retro-rocket/src/features/boards/retrospective/components/DragDropColumn.tsx`'s populated (drag-and-drop) rendering per the selected direction — the empty-column state is owned by `GroupableColumn.tsx` (Phase 4, User Story 2), not by `DragDropColumn.tsx` itself, per current verified behavior — **Done, minimal change**: its card-list spacing (`space-y-3`) already matched the reviewed prototype exactly; the actual card visual treatment lives in `DraggableCard.tsx` (T024). 27/27 passing, unchanged.
- [X] T026 [P] [US1] Rebuild `retro-rocket/src/features/boards/retrospective/components/RetrospectiveBoard.tsx` (the column-grid orchestrator) per the selected direction, preserving `useBoardGridColumns`-driven column sizing at 3 or 4 columns, the `showActionColumn` toggle wiring, and stacking below the responsive breakpoint (FR-006, `data-model.md`'s `Board Layout State`, `contracts/functional-parity-contract.md`) — **Done** (closes the G1 gap `/speckit-analyze` found): each column now sits in a translucent, role-tinted gradient panel (`rounded-3xl bg-gradient-to-b {role-accent} to-surface-raised/30 backdrop-blur-sm`), matching the reviewed Direction C prototype's per-column treatment; `useBoardGridColumns` sizing, the `showActionColumn` toggle, and breakpoint stacking are all unchanged/preserved (presentation-only per FR-017). 14/14 passing; type-check/lint clean.
- [X] T027 [P] [US1] Rebuild `retro-rocket/src/features/boards/participants/components/ResponsiveParticipantDisplay.tsx`, `CompactAvatarGroup.tsx`, `ParticipantPopover.tsx`, `UserAvatar.tsx` per the selected direction — **Done**, found and fixed two real pre-existing defects while rebuilding: (1) `ParticipantPopover`'s panel was a flat opaque `bg-surface-raised border shadow-lg` — now `bg-surface-raised/95 backdrop-blur-xl border-border-default/40 shadow-2xl rounded-2xl`, matching Direction C's glass language; (2) `UserAvatar`'s image `alt` text was hardcoded Spanish (`` `Avatar de ${name}` ``, an FR-013 i18n violation) and its initials background used the legacy `bg-gradient-to-br from-primary-500 to-blue-600` palette instead of a semantic token — both fixed (new `participants.avatarOf`/`participants.viewAll` i18n keys added to en/es, background now `bg-action`/`text-text-inverse`); `CompactAvatarGroup`'s own hardcoded `title` string fixed the same way. `ResponsiveParticipantDisplay` is a thin orchestrator, unchanged.
- [X] T028 [US1] Using the `animate` skill (Constitution Principle IX), implement real-time card-entrance motion for cards added by other participants in `DragDropColumn.tsx`/`DraggableCard.tsx`, gated by the existing `useReducedMotion` hook (`research.md` §4, FR-015) — **Done**: consulted the `animate` skill directly. Gate: occasional-tier, purpose "preventing a jarring change" (remote arrivals) + "feedback" (own submissions) — same treatment serves both, no differentiation (avoids speculative complexity). Properties already correct (opacity+y transform, AnimatePresence-driven exit); the one real gap was the missing mandated strong ease-out curve (relying on framer-motion's weak default) — added `ease: [0.23, 1, 0.32, 1]` at the existing 180ms duration. Reduced motion already handled app-wide by the root `<MotionConfig reducedMotion="user">` (feature 028) — no per-component gating needed, same precedent as 031's T042.
- [X] T029 [US1] Using the `animate` skill, implement drag-lift/drop feedback motion on `DraggableCard.tsx`, gated by `useReducedMotion` (`research.md` §4, FR-015) — **Done**: purpose feedback (confirms grab) + spatial consistency (shows elevation); gesture motion → spring, not a tween. Added `initial={{scale:1,y:0}} animate={{scale:1.03,y:-4}} transition={{type:'spring',duration:0.3,bounce:0.15}}` wrapping the `DragOverlay`'s rendered clone in `DragDropColumn.tsx` (the actual dragged visual dnd-kit follows the pointer with) — the existing shadow-swap-on-mount gave instant strong feedback but no interpolated "lift" motion to sell the physicality; the in-list ghost placeholder (opacity 0.5, unchanged) correctly gets no lift since it isn't what's being manipulated. Reduced motion inherited from the app-root `MotionConfig` as above.
- [X] T030 [US1] Verify the SC-001/SC-007 performance/scale budget — card interactions responding within 100ms, drag-and-drop sustaining at least 50fps with no dropped-frame stalls in a column holding 30+ cards, and real-time updates rendering without perceptible lag at up to 15 concurrent participants — per `quickstart.md` §3, using the T007 seeding helper and Chrome DevTools' Performance panel; adjust T021-T029 if either threshold is exceeded — **Done** (throwaway Playwright script against the real emulator-backed dev server with 32 seeded cards in one column, deleted after use, matching the T008 precedent — no headed Chrome DevTools session available in this environment, so `requestAnimationFrame`-based in-page instrumentation substituted for the same measurements): drag-and-drop sustained **60.7fps** (target ≥50fps) with a **32ms** max frame gap (no stalls) across a 1.2s drag gesture. Press-feedback latency (LikeButton's `whileTap`, the actual "visual response") averaged **~57ms** (26-81ms range) — well under the 100ms budget. **Genuine finding**: the *confirmed* round-trip latency (count updating after the full backend+WebSocket-relay round trip) ran 74-404ms — slower than 100ms on a cold connection. This is because `useOptimizedCards.ts` is deliberately non-optimistic (its own docstring: "no client-side optimistic mutation is needed anymore" — every write commits via the backend then relays back live) — a pre-existing architectural fact from features 019/021, not something this presentation-only feature may change (FR-017). SC-001's "visually respond" is satisfied by the immediate press feedback, not full round-trip confirmation; this distinction is worth keeping in mind for any future feature that revisits real-time write latency, but is out of scope here.

**Checkpoint**: User Story 1 is independently functional — card CRUD, vote/
like/react, presence/typing, and the column-grid container all work through
the redesigned surface at the validated scale.

---

## Phase 4: User Story 2 - Organize and Synthesize Cards (Priority: P2)

**Goal**: A participant can drag cards to reorder/move them, group related
cards manually or via AI suggestion, and collapse/disband groups, through
the selected visual direction.

**Independent Test**: On a board with several cards, drag a card to reorder
and to move it across columns; create a manual group; run AI-suggested
grouping and review a suggestion; collapse/expand and disband a group.

### Tests for User Story 2 ⚠️

- [X] T031 [P] [US2] Update `retro-rocket/src/test/features/boards/clustering/GroupableColumn.test.tsx`'s grouping-specific assertions, `GroupCard.test.tsx`, `GroupedCardList.test.tsx` to match the new markup, preserving group formation/collapse/expand/disband assertions (FR-005) — **Done**: updated 9 stale `/crear tarjeta/i`/`/cancelar/i` regex assertions across the 3 `GroupableColumn` test files to match the now-wired-up i18n keys; rewrote `GroupCard.test.tsx`'s disband-button tests to drop the now-unnecessary `fireEvent.mouseEnter` (see T035's real defect fix) and added a regression guard proving the button renders with no hover at all. 78 (GroupableColumn ×3) + 33 (GroupCard) + 5 (GroupedCardList) = 116 tests passing.
- [X] T032 [P] [US2] Update `retro-rocket/src/test/features/boards/clustering/ColumnHeaderMenu.test.tsx`, `GroupSuggestionModal.test.tsx` to match the new markup, preserving the suggest-then-review-before-apply assertions (FR-005) — **Done**: folded into T036/T037 above (updated together with their component rebuilds, same as T031's approach). 26 (ColumnHeaderMenu) + 5 (GroupSuggestionModal) passing.
- [X] T033 [US2] Update `retro-rocket/e2e/retrospective-board.spec.ts` drag-and-drop-across-columns and grouping assertions for the new structure (FR-004, FR-005) — **Done, no changes needed**: ran the full spec against the real emulator-backed dev server after T034-T038's rebuild — 32/32 passing unmodified, including grouping (create/remove-member/disband) and column-grouping-preference propagation.

### Implementation for User Story 2

- [X] T034 [US2] Rebuild `retro-rocket/src/features/boards/clustering/components/GroupableColumn.tsx` (column + grouping orchestration, including its empty-column state) per the selected direction (depends on T025's `DragDropColumn.tsx` rebuild) — **Done**: stripped the column's own opaque `<Card variant="outlined">` header (now redundant/conflicting since it sits inside `RetrospectiveBoard.tsx`'s new translucent gradient panel, T026) down to a minimal icon/title/count row; new-card form restyled from a dashed-border `Card` to a `rounded-2xl bg-surface-raised/70 backdrop-blur-sm` glass box matching the reviewed prototype; fixed 3 hardcoded Spanish strings ("Vista previa del color", "Crear tarjeta", "Cancelar") that had existing, unused i18n keys — just never wired up. 78 tests (3 files) passing.
- [X] T035 [P] [US2] Rebuild `retro-rocket/src/features/boards/clustering/components/GroupCard.tsx`, `GroupedCardList.tsx` per the selected direction — **Done**, found and fixed two real defects: (1) `GroupCard`'s disband/"ungroup" button was gated behind React `isHovered` state (`onMouseEnter`/`onMouseLeave`) — not just visually hidden but **entirely absent from the DOM** until a mouse hovered, unreachable via keyboard/touch no matter how a user tabbed (stricter than the opacity-0 class of defect fixed earlier); now always rendered. (2) `GroupedCardList`'s group-separator line hardcoded `from-slate-300 dark:from-slate-600` — a manual `dark:` variant bypassing the semantic token system entirely — now `from-border-default` (theme-aware automatically). Also fixed a `bg-action text-white` → `text-text-inverse` token mismatch on the "primary card" badge. rounded-xl → rounded-2xl for corner-radius consistency with the rest of Direction C.
- [X] T036 [US2] Rebuild `retro-rocket/src/features/boards/clustering/components/ColumnHeaderMenu.tsx` per the selected direction, adopting the shared overlay hook from T013 — **Done**, the first of the four T013 target menus migrated. Found and fixed two real defects while rebuilding: (1) no Escape-key dismissal at all (hand-rolled outside-click-only listener) — a direct FR-012 gap, now fixed by construction via `useBoardMenuOverlay`'s `useDismiss`; (2) the trigger's `title`/`aria-label` were hardcoded English literals ("Group cards"/"Grouping options") despite the rest of the component correctly using `t()` — now wired to two new i18n keys (`retrospective.grouping.menuTrigger`/`menuLabel`, en/es). Glass panel treatment (`bg-surface-raised/95 backdrop-blur-xl rounded-2xl shadow-2xl`) matching Direction C. Updated 4 test assertions: `aria-haspopup` changed from the hand-rolled `"true"` to Floating UI's more precise `"menu"`, label/title assertions now check the i18n key (matching the suite-wide `t()` mock convention), and the outside-click test now fires `pointerDown` (Floating UI's actual default `outsidePressEvent`) instead of `mouseDown`. 227/227 passing across the whole clustering folder; type-check/lint clean.
- [X] T037 [US2] Rebuild `retro-rocket/src/features/boards/clustering/components/GroupSuggestionModal.tsx` per the selected direction, preserving the suggest-then-review-then-apply flow — **Done**, found and fixed the largest batch of real defects yet in this feature: (1) **no Escape-key dismissal at all** — only closed on backdrop click, a direct FR-12 gap, now fixed with a real `keydown` listener (added 2 regression tests); (2) **~15 hardcoded Spanish strings** (title, subtitle, empty state, show/hide-cards, reason/algorithm/keywords labels, discard/close buttons, footer hint, and the "Alta"/"Media"/"Baja" similarity labels) — all wired to i18next, adding 14 new keys and, along the way, **fixing a pre-existing en/es drift bug** (`groupSuggestion.group`/`cardsInGroup` existed only in `es.json`, meaning English would have silently shown a raw key or fallback); (3) the header's `bg-gradient-to-r from-blue-50 dark:from-blue-900/20 ...` — hardcoded palette + manual `dark:` variant, same defect class as T035's fix — replaced with Direction C's glass panel treatment; (4) `text-yellow-600 bg-yellow-50` and `ring-blue-200` hardcoded colors → `text-warning-fg bg-warning-bg` / `ring-info-fg/40` semantic tokens. Added `role="dialog"`/`aria-modal`/`aria-label`. 229/229 passing across the clustering folder; type-check/lint clean.
- [X] T038 [US2] Using the `animate` skill, implement group collapse/expand motion in `GroupCard.tsx`, gated by `useReducedMotion` (FR-015) — **Done**: consulted the `animate` skill. Gate: occasional/click-triggered, purpose "state indication". `height` confirmed as the sanctioned accordion exception (no transform equivalent for this reveal). Same recurring gap as T028/T029 — missing the mandated strong ease-out curve — fixed by adding `ease: [0.23, 1, 0.32, 1]` at the existing 200ms duration. Reduced motion inherited from the app-root `MotionConfig`. 33/33 passing.

**Checkpoint**: User Stories 1 AND 2 both work independently — drag-and-drop
and grouping behave identically to before, restyled to the selected
direction.

---

## Phase 5: User Story 3 - Facilitate the Session (Priority: P3)

**Goal**: The board owner can control the countdown timer, sentiment
analysis, team mood dashboard, and private notes via the owner-only
facilitator menu, and manage action items — all restricted to the owner and
absent (not disabled) for non-owners.

**Independent Test**: As the board owner, open the facilitator menu,
start/pause/reset the timer, enable sentiment analysis, view team mood,
write a private note, convert a card to an action item, and create/edit/
delete an action item; as a non-owner, confirm the facilitator menu and
convert-to-action control are both absent.

### Tests for User Story 3 ⚠️

> The ARIA-tabs assertion in T040 must FAIL against the current div-based
> tab markup before T046 makes it pass.

- [X] T039 [P] [US3] Update `retro-rocket/src/test/features/boards/countdown/CountdownTimer.test.tsx`, `useCountdown.test.ts` for the new markup, preserving real-time timer-state assertions (FR-009) — **Done**: 4 status-text assertions updated from hardcoded Spanish literals to the now-correctly-wired i18n keys (see T045); 1 class assertion updated for the refined pill styling. 33/33 passing.
- [X] T040 [P] [US3] Update `retro-rocket/src/test/features/boards/facilitator/FacilitatorMenu.test.tsx` and `retro-rocket/src/test/features/boards/facilitator/FacilitatorMenuTabs.test.tsx` for the new markup and add an accessible ARIA-tabs-pattern assertion (`role="tablist"`/`role="tab"`/`role="tabpanel"` or equivalent) — write it to FAIL against the current markup first, then pass after T046 (`contracts/accessibility-interaction-contract.md`) — **Done**: fixed a real regression I introduced mid-rebuild (dropped `aria-label` when migrating to the shared hook, keeping only `title` — `getByLabelText` doesn't match `title`) caught immediately by the existing test suite; added 2 new tests confirming the real `role="tablist"`/`"tab"`/`"tabpanel"` pattern (`aria-selected`, `aria-controls`↔`id` linking, `tabindex` roving) plus ArrowRight/ArrowLeft keyboard navigation. 24 (FacilitatorMenu+CountdownFeatureDemo) + 4 (FacilitatorMenuTabs) passing.
- [X] T041 [P] [US3] Create `retro-rocket/src/test/features/boards/facilitator/ControlsTab.test.tsx`, `SentimentTab.test.tsx`, `TeamMoodTab.test.tsx` (no unit test currently exists for these three) covering each tab's rendered content and controls; update `retro-rocket/src/test/features/boards/facilitator/NotesTab.test.tsx` for the new markup, preserving the owner-private-note assertions (FR-009) — **Done**: 3 new test files (5+5+3 = 13 tests), each covering the tab's core states/interactions plus a regression guard for its real defect fix (SentimentTab's new `role="switch"`, NotesTab's now-always-visible edit/delete). 42/42 passing across the facilitator folder.
- [X] T042 [P] [US3] Update `retro-rocket/src/test/features/boards/retrospective/ActionItemCard.test.tsx`, `ActionItemsColumn.test.tsx`, `ActionColumnToggle.test.tsx` for the new markup, preserving create/edit/delete/toggle assertions (FR-007) — **Done**: 65/65 existing tests passed unmodified; added 1 new regression guard proving the edit/delete controls are never `opacity-0` (see T048's hover-only fix). 66 total passing.
- [X] T043 [P] [US3] Create `retro-rocket/src/test/pages/RetrospectiveTopbar.test.tsx` (no unit test currently exists) covering the title/participant display and the countdown-timer mount point — options-menu assertions (export/copy/share/exit) are added separately in T052 once that markup is rebuilt — **Done**, first-ever test file for this component. Covers the pre-load placeholder state and the loaded state (title, `ResponsiveParticipantDisplay` mount with the correct participant count, `CountdownTimer` mount, `FacilitatorMenu` mount) with those three children mocked to lightweight stubs — each already has its own dedicated test suite (`CountdownTimer.test.tsx` 33 tests, `FacilitatorMenu.test.tsx`), so this file only asserts the topbar composes them correctly, not their internals. Deliberately does not touch the still-hand-rolled options-menu markup (`getBoundingClientRect` positioning, `mousedown`/Escape listeners) — that gets migrated to `useBoardMenuOverlay` in T051 (Phase 6/US4), and its assertions are added in T052 against the rebuilt markup. 2/2 passing; type-check/lint clean.
- [X] T044 [US3] Update `retro-rocket/e2e/facilitator-countdown.spec.ts` selectors, preserving timer start/pause/reset and owner-only-visibility assertions (FR-009) — **Done**, verified against the real Firebase emulators (`firebase emulators:exec --project demo-retrorocket --only auth,firestore`) — passes unchanged, no selector edits needed. All of its text/role selectors (`Facilitador` trigger, `Temporizador` heading, `5min` preset, `Crear Temporizador`, `Iniciar`/`Pausar`/`Reiniciar`/`Eliminar` buttons) resolve to i18n keys that were untouched by the T045/T046/T047 rebuilds — only the *English* locale's `facilitator.tabs.*` namespace was empty (fixed in T046); the Spanish strings this spec asserts against were already correct. Confirmed the default-open facilitator tab is still `controls` (ControlsTab, which renders the countdown UI this spec drives) despite T046's new ARIA-tabs rewrite. 1/1 passing.

### Implementation for User Story 3

- [X] T045 [US3] Rebuild `retro-rocket/src/features/boards/countdown/components/CountdownTimer.tsx` per the selected direction, preserving real-time state display (FR-009) — **Done**, found and fixed a real defect: `getStatusText()` hardcoded all four Spanish status literals directly ("Tiempo terminado", "Pausado", "En curso", "Detenido") despite matching i18n keys (`retrospective.facilitator.countdown.status.*`) already existing and used correctly elsewhere (`ControlsTab.tsx`) — this component just never called `t()` at all. Also fixed a pre-existing en/es drift bug: `countdown.totalTime` existed only in `es.json`. Restyled from a heavy `border-2` box to a compact glass pill (`bg-surface-raised/80 backdrop-blur-sm rounded-full`, `tabular-nums`) fitting the topbar dock. 33/33 passing; type-check/lint clean.
- [X] T046 [US3] Rebuild `retro-rocket/src/features/boards/countdown/components/FacilitatorMenu.tsx` and `retro-rocket/src/features/boards/facilitator/components/FacilitatorMenuTabs.tsx` per the selected direction, adopting the shared overlay hook (T013) and an accessible ARIA-tabs pattern, preserving the existing `if (!isOwner) return null;` guard (FR-009, `contracts/accessibility-interaction-contract.md`) — **Done**, the second of the four T013 menus migrated, and the biggest defect batch in this component: (1) manual `getBoundingClientRect`-based positioning, `mousedown` outside-click, and Escape-key listeners all replaced by `useBoardMenuOverlay({role:'dialog'})`; (2) **`en.json`'s entire `facilitator.tabs` namespace was empty** — every tab label rendered untranslated/missing in English; now populated (Timer/AI/Team Mood/Controls/Notes); (3) the tab bar had **zero ARIA tab semantics** — plain buttons, no `role="tablist"`/`"tab"`/`"tabpanel"`, no `aria-selected`/`aria-controls` — now a real WAI-ARIA tabs pattern with roving `tabindex` and ArrowRight/ArrowLeft navigation; (4) removed a dead/broken `getCompactLabel()` lookup table whose hardcoded keys (`'Timer'`, `'IA'`, ...) didn't even match the real translated values (e.g. the `controls` tab's actual label is "Controles", not in the map) — relies on existing `truncate` CSS instead; (5) hardcoded `bg-gradient-to-r from-blue-50 ... dark:from-slate-800 ...` (manual `dark:` variant) and `bg-blue-500`/`bg-red-500` badge colors → semantic tokens + Direction C glass panel. `isOwner` guard preserved unchanged.
- [X] T047 [P] [US3] Rebuild `retro-rocket/src/features/boards/facilitator/components/ControlsTab.tsx`, `SentimentTab.tsx`, `TeamMoodTab.tsx`, `NotesTab.tsx` per the selected direction, preserving each tab's existing behavior (FR-009) — **Done**, largest defect batch across a single task in this feature: (1) **NotesTab** — hover-only edit/delete buttons (same defect class as T021/T024, now fixed) plus a hardcoded gradient with manual `dark:` variant; (2) **ControlsTab** — `getProgressBarColor()` and two `text-slate-400` icons hardcoded palette instead of tokens, a timer-status panel with the same manual-`dark:`-gradient defect, Start/Pause buttons hardcoding `bg-green-600`/`bg-yellow-500 text-white` (removed in favor of the already-verified-safe `variant="primary"`/`"secondary"` styling — button-fill contrast can't be safely improvised without a token audit), and a quick-timer-preset button whose hover state was visually identical to its resting state (dead hover, fixed); (3) **SentimentTab** — 5 status icons hardcoded `text-blue-500`/`text-red-500`/etc. instead of the matching semantic-token `color` field sitting right next to each in the same object, a hardcoded Spanish `title` on the auto-analysis toggle, that toggle having **zero switch semantics** (`role="switch"`/`aria-checked` now added), and the "Advanced Settings" disclosure button having the same dead-hover bug as ControlsTab's preset buttons; (4) **TeamMoodTab** — 3 hardcoded `bg-blue-500` loading dots → `bg-info-fg`. 42/42 passing; type-check/lint clean.
- [X] T048 [P] [US3] Rebuild `retro-rocket/src/features/boards/retrospective/components/ActionItemsColumn.tsx`, `ActionItemCard.tsx`, `ActionColumnToggle.tsx` per the selected direction (FR-007) — **Done**, found and fixed 5 real defects: (1) `ActionItemCard`'s edit/delete buttons were `opacity-0 group-hover:opacity-100` with no `focus-within` — same recurring hover-only defect class, now always visible; (2) its edit button's hover color (`hover:bg-warning-bg`) was identical to the card's own resting background (`bg-warning-bg`) — a dead hover, now `hover:bg-surface-raised`; (3) `ActionItemsColumn`'s "Add action item" dashed-border button had `hover:border-warning-fg` identical to nothing changing since the border was already that color at a different opacity — softened the resting border to `/50` so hover has somewhere to go; (4) two `bg-amber-600 hover:bg-amber-700 text-white` hardcoded buttons (Save in both files) → `variant="primary"`; (5) **`ActionItemsColumn` double-boxed itself** — it already had its own opaque `bg-warning-bg/60 border shadow` wrapper, now redundant/conflicting since `RetrospectiveBoard.tsx` (T026) wraps it in the same warning-tinted translucent panel — stripped to a bare header, same fix as `GroupableColumn.tsx` (T034). `ActionColumnToggle.tsx` was already correct (real `@headlessui/react Switch`, proper tokens) — unchanged. 66/66 passing; type-check/lint clean.
- [X] T049 [US3] Rebuild `retro-rocket/src/features/boards/retrospective/components/CardMenu.tsx`'s convert-to-action-item control per the selected direction, adopting the shared overlay hook (T013), preserving owner-only visibility (FR-007) — **Done**, the fourth and final of the four T013 menus migrated: hand-rolled `getBoundingClientRect` positioning, `mousedown` outside-click, and its own Escape-key listener replaced by `useBoardMenuOverlay({placement:'right'})` + `FloatingPortal`/`FloatingFocusManager`. Fixed 3 defects: (1) hardcoded `bg-amber-600 hover:bg-amber-700 text-white` Convert button → `bg-action hover:bg-action-hover text-text-inverse`; (2) dead hover on the trigger button (`hover:bg-surface-raised` identical to the card's own background) → `hover:bg-surface`; (3) same dead-hover bug on the Cancel button, same fix. No test file existed for this component at all (`DraggableCard.test.tsx`'s 37 passing tests mock `CardMenu` entirely, providing zero coverage of its internals) — created `CardMenu.test.tsx` (5 new tests: owner-only absence, open-on-click, convert with resolved assignee name, cancel discards, Escape-to-close). 5/5 passing; type-check/lint clean.
- [X] T050 [P] [US3] Rebuild `retro-rocket/src/features/boards/sentiment/components/SentimentBadge.tsx`, `SentimentProgressBar.tsx`, `TeamMoodDashboard.tsx`, `SentimentFilter.tsx` per the selected direction — **Done**, found and fixed defects in every file plus two shared color-source constants: (1) `types/sentiment.ts`'s `SENTIMENT_COLORS` and `types/teamMood.ts`'s `getMoodScoreColor`/`getMoodScoreBgColor` both hardcoded palette classes with manual `dark:` variants instead of semantic tokens — fixed, and the mood-score bands were also re-aligned to `getMoodScoreLabel`'s own thresholds (≥7.5/≥4.5) so color and label now agree on where "concerning" starts, matching the existing ≈4.6-neutral-board anchor comment; (2) `SentimentProgressBar.tsx` — `bg-green-500`/`bg-slate-400`/`bg-red-500` → tokens, plus `transition-all` (named-properties violation) → `transition-[width]` with the missing strong ease-out curve added (same recurring animation gap as T045/T046/T047/T048); (3) `TeamMoodDashboard.tsx` — 6 hardcoded icon/spinner colors (`text-green-500` etc.) → tokens; 8 `motion.div` entrances had no explicit duration/easing (weak framer-motion default, same recurring gap) and the insight-cards list animated with zero stagger (`Never Ship` rule) — both fixed; (4) `SentimentFilter.tsx` — 4 hardcoded Spanish literals ("Todos", "Positivos", "Filtrar por sentimiento", "Close filter menu" in English) replaced with i18n (`sentiment.positive/negative/neutral` already existed; added new `sentiment.filter.{all,filterBySentiment}` to en.json/es.json), a dead-hover dropdown-item bug (`hover:bg-surface-raised` identical to the dropdown's own `bg-surface-raised`) fixed, `bg-blue-500` selection dot → token, and Escape-to-close was previously wired only to a backdrop button's `onKeyDown` (inert unless that invisible button had focus) — replaced with a real document-level Escape listener; (5) `SentimentBadge.tsx` — `transition-all` → `transition-opacity` (named property), and its override-cycling button had no visible keyboard-focus indicator — added `focus-visible:ring`. Scope note: `SentimentFilter` is exported but not imported anywhere in the live app (confirmed via grep) — fixed for correctness/consistency since it's explicitly named in this task, but no new test file was added for it given zero production usage. `SentimentBadge` (used live in `DraggableCard.tsx`/`FacilitatorMenu.tsx`) and `TeamMoodDashboard` (used live in `TeamMoodTab.tsx`, but fully mocked by `TeamMoodTab.test.tsx` — zero real coverage, same gap pattern as T049's CardMenu) each got a new test file (4 tests each). `SentimentProgressBar` has no direct test but is exercised for real through the new `TeamMoodDashboard.test.tsx`. 94/94 relevant tests passing (86 in `sentiment/`, 4 new `SentimentBadge`, 4 new `TeamMoodDashboard` — counted within the 86 — plus 37/37 `DraggableCard.test.tsx` confirmed unaffected); type-check/lint clean.

**Checkpoint**: User Stories 1-3 all work independently — facilitator-only
controls are fully functional and correctly restricted to the owner.

---

## Phase 6: User Story 4 - Export and Wrap Up (Priority: P4)

**Goal**: A participant can export the board to PDF/DOCX/TXT (with
facilitator-only options for the owner), copy the board ID, share a link,
and exit to the dashboard, through the selected visual direction.

**Independent Test**: Open the export option, export to each format with
visible progress/success feedback; as owner, confirm facilitator-only
export options are available and absent for a non-owner; copy the board ID
and a share link; exit to the dashboard.

### Tests for User Story 4 ⚠️

- [X] T051 [P] [US4] Update `retro-rocket/src/test/features/boards/export/ImprovedExportPopover.test.tsx` for the new markup, preserving format-selection/facilitator-only-options/progress/success/error assertions (FR-010) — **Done**, all 4 pre-existing tests passed unmodified against the rebuilt markup; added 2 new regression guards for the two real defects fixed in T055 (`role="dialog"`/`aria-modal`/`aria-labelledby`, and `aria-pressed` on the format-selection buttons). 6/6 passing.
- [X] T052 [US4] Add options-menu assertions (export trigger, copy ID, share link, exit) to `retro-rocket/src/test/pages/RetrospectiveTopbar.test.tsx` (created in T043), preserving the existing toast-confirmation behavior (FR-011) — **Done**, 5 new tests against the real (unmocked) `useBoardMenuOverlay` hook via `userEvent`: menu opens with all 4 items, copy-ID and share both call `navigator.clipboard.writeText` with the right payload and show the existing toast confirmation, exit navigates to `/dashboard`, and Escape closes it. 7/7 passing (2 pre-existing + 5 new).
- [X] T053 [US4] Update `retro-rocket/e2e/export.spec.ts` selectors, preserving format/facilitator-only-option/progress/success/error assertions (FR-010) — **Done**, verified against the real Firebase emulators — passes unchanged (2/2), no selector edits needed; none of its text/role/title selectors (`Opciones`, `Exportar`, format labels, `Exportar {format}` dynamic button text, `button[title="Cerrar"]`) were altered by the T054/T055 rebuilds, only surrounding classes/tokens/ARIA attributes changed.
- [X] T054 [US4] Rebuild `retro-rocket/src/features/boards/retrospective/components/RetrospectiveTopbar.tsx`'s options menu (export trigger, copy ID, share, exit) per the selected direction, adopting the shared overlay hook (T013) (FR-011) — **Done**, the fifth and final of the board's menus migrated to `useBoardMenuOverlay` (closing out the T013 consolidation: ColumnHeaderMenu, FacilitatorMenu, CardMenu, and now this topbar menu). Replaced hand-rolled `getBoundingClientRect` positioning, `mousedown` outside-click, and its own Escape-key/focus-timeout logic with the shared hook + `FloatingPortal`/`FloatingFocusManager`. Also fixed: `transition-all` on the trigger button → named properties; menu items restyled from a flat `bg-surface-raised` box to the Direction C glass panel (`bg-surface-raised/95 backdrop-blur-xl`, `rounded-2xl`) matching the other 4 migrated menus.
- [X] T055 [US4] Rebuild `retro-rocket/src/features/boards/export/components/ImprovedExportPopover.tsx` per the selected direction, preserving format selection, facilitator-only options, and progress/success/error feedback (FR-010) — **Done**, found and fixed 5 real defects: (1) the modal had **no dialog semantics at all** — no `role="dialog"`/`aria-modal`, and its own `id="export-dialog-title"` was never referenced via `aria-labelledby`; (2) the initial-focus `useEffect` called `.focus()` on the popover's container `div`, which had no `tabIndex` — a no-op in every browser, so keyboard focus never actually landed in the dialog on open — added `tabIndex={-1}`; (3) format-selection buttons (a single-select icon-button group) had no `aria-pressed`, so the selected format wasn't exposed to assistive tech — added; (4) a dead-hover close (X) button (`hover:bg-surface-raised` identical to the popover's own `bg-surface-raised`) → `hover:bg-surface`; (5) hardcoded `border-blue-500` (mixed with the already-correct `bg-info-bg` token) on the selected format, and a hardcoded `bg-blue-600 hover:bg-blue-700 text-white` Export button → `border-info-fg` token and `variant="primary"`. Restyled to the Direction C glass panel (`bg-surface-raised/95 backdrop-blur-xl`, `rounded-2xl`).
- [X] T056 [US4] Using the `animate` skill, implement the options-menu and export-popover open/close transitions in the components touched by T054-T055, gated by `useReducedMotion` (FR-015) — **Done**, consulted the `animate` skill directly. Gate: occasional-tier throughout (deliberate clicks, not high-frequency), purposes named (menu = spatial consistency, trigger-icon rotation = state indication, modal entrance = preventing a jarring change, status banners = feedback). Fixed the recurring missing-ease-out gap on 4 transitions (options-menu dropdown 160ms, trigger-icon rotation 200ms, export-modal entrance bumped 150ms→200ms, and the error/success status banners which previously had **no transition prop at all**) — all now `ease: [0.23, 1, 0.32, 1]`, all ≤200ms, none exceeding the modal 200–500ms guideline. Reduced motion inherited for free from the app-root `MotionConfig reducedMotion="user"` (feature 028) — no per-component gating needed. One finding deliberately left unfixed and flagged for T062: none of the 4 now-migrated anchored menus (including this one) set `transform-origin` at the trigger (defaults to center), a "Never Ship" item for trigger-anchored popovers — fixing it only here would make this menu inconsistent with its 3 siblings; proper fix belongs in T062's cross-cutting `review-animations` pass.

**Checkpoint**: User Stories 1-4 all work independently — export and
wrap-up actions behave identically to before, restyled to the selected
direction.

---

## Phase 7: User Story 5 - Consistent, Accessible Experience for Every Participant (Priority: P5)

**Goal**: The redesigned board remains legible, operable, and coherent
across themes, locales, motion preferences, and viewport sizes, for every
state introduced or touched by Stories 1-4.

**Independent Test**: Load the board on narrow mobile and ultra-wide
desktop viewports, in both themes, in both languages, and with reduced
motion enabled — every capability from Stories 1-4 remains available and
legible.

### Tests for User Story 5 ⚠️

- [X] T057 [P] [US5] Extend `retro-rocket/e2e/accessibility.spec.ts` with a new `/retro/:id` theme × state matrix (loading, populated, empty-column, error) — this is net-new coverage closing the gap found in `research.md` §6 (FR-014, SC-003) — **Done**, verified against real Firebase emulators (12/12 passing across both themes). The pre-existing "Board (cards/columns/voting)" test only ever scanned a freshly-created empty board — relabeled as the "empty-column" state and kept unchanged — then added 3 genuinely new states × 2 themes: "populated" (adds a real card + a vote, exercising `DraggableCard`/`CardFooter`/`CardMenu` DOM the empty scan never touched), "loading" (holds the `GET /api/retrospectives/:id` fetch open), and "error" (aborts that same request to drive the generic-error branch, distinct from the already-covered board-deleted/`notFound` branch). Building this test surfaced and fixed a real, live defect in `RetrospectivePage.tsx`: its loading/notFound/error early-return states used a hardcoded `bg-gradient-to-br from-purple-50 via-pink-50 to-indigo-50` background with **no `dark:` variant** — in dark theme this left light-colored text on a light-colored background, which is exactly the class of bug this new dark-theme scan exists to catch. Fixed to match the main view's already-correct `from-slate-50 to-blue-50 dark:from-slate-900 dark:to-blue-950`. Also fixed 2 hardcoded Spanish strings in the error branch ("Retrospectiva no encontrada"/"No se pudo encontrar...") that had no i18n key at all despite the adjacent `notFound`-sibling `boardDeleted` branch already being correctly wired — added `retrospectivePage.notFound.{title,message}` to en.json/es.json, updated `RetrospectivePage.test.tsx`'s 3 assertions accordingly (6/6 passing).
- [X] T058 [P] [US5] Add keyboard-only and touch-emulated operability assertions for every board menu (options, facilitator, card, column header, export, reaction picker) to their respective e2e specs, per `contracts/accessibility-interaction-contract.md` (FR-012, SC-004) — **Done**, added 2 new e2e tests to `accessibility.spec.ts`: keyboard (Tab-to-reach via `.focus()`, Enter to open, Escape to dismiss) and touch (`.tap()`, no prior hover) coverage for the options menu, facilitator menu, card menu, and column-header (grouping) menu, plus the export popover the options menu opens into. The reaction picker was already covered by the pre-existing keyboard test in this file. Verified against real Firebase emulators (14/14 passing including the T057 matrix). This work surfaced two real, live defects, both fixed: (1) 3 of the 4 `useBoardMenuOverlay`-migrated menus (`FacilitatorMenu`'s `role="dialog"` panel, `CardMenu`, and the topbar options menu) had **no `aria-label` on their floating content panel itself** — only their trigger buttons were labeled — leaving an unnamed dialog/menu for assistive tech; brought in line with `ColumnHeaderMenu`, the one menu that already did this correctly. (2) Building the touch test at a `1024×900` viewport (the `lg` grid-breakpoint boundary, where the board's 4-column grid is at its narrowest) revealed `GroupableColumn.tsx`'s column-title `<h2>` collapsing to **literal 0px width** — invisible and unlocatable — because its `shrink-0` icon/badge siblings and the header row's `shrink-0` action-button toolbar (a grouping-menu button + a labeled "Agregar" button) left it no room; `truncate`'s `white-space:nowrap` removes the normal min-content shrink floor, so flexbox shrank it all the way to zero instead of truncating to a few characters. Fixed by making the "Agregar" button icon-only below `xl` (matching the `hidden lg:inline`-label pattern already used elsewhere in this feature) and giving the title's wrapper `flex-1` so it claims the (now larger) remaining space — confirmed via a throwaway debug script (not committed) that the title now renders at a real, non-zero width. 229/229 clustering unit tests + type-check/lint remain clean.
- [X] T059 [P] [US5] Add responsive-viewport checks (narrow mobile, ultra-wide desktop) for the rebuilt board to `retro-rocket/e2e/retrospective-board.spec.ts` or a dedicated responsive spec, confirming columns stack rather than forcing horizontal scroll (FR-016) — **Done**, created a new dedicated `e2e/board-responsive.spec.ts` rather than appending to the already-1300+-line `retrospective-board.spec.ts` (that file is scoped to realtime/backend behavior, not layout). 3 tests, verified against real Firebase emulators: narrow mobile (375×812 — confirms `grid-cols-1` stacking, second column's heading sits below not beside the first, plus `expectNoHorizontalOverflow`), ultra-wide desktop (2560×1440 — confirms `lg:grid-cols-N` side-by-side layout and no overflow), and a regression pin at the exact `lg` breakpoint boundary (1024px) for the 0px-collapsed-title defect found and fixed during T058. One infra snag: `signInWithGoogle()` waits for a header element hidden below the `md` breakpoint (same constraint `dashboard-manage.spec.ts` already works around) — fixed by signing in and creating the board at a default viewport, then `page.setViewportSize()` down to the narrow-mobile size only for the layout assertions. 3/3 passing.
- [X] T060 [P] [US5] Add reduced-motion assertions exercising the primary flows (add/vote/react to a card, drag-and-drop, group, open every menu, export, start/pause the countdown) to `retro-rocket/e2e/accessibility.spec.ts`'s reduced-motion emulation, consistent with the pattern established in features 028/029/031 (FR-015) — **Done**. The pre-existing "P1 core flow" reduced-motion test (spec 028) is named "add card, vote, group" but its body only ever exercised add-card and vote, never grouping — left that test unchanged (still valid coverage for what it does test) and added a new one covering everything this feature actually touched: keyboard drag-and-drop (dnd-kit's Space/Arrow/Space sequence), grouping (select "group by user", confirm the group header appears), all 5 board menus opening/closing, the export popover reaching its open state, and countdown start/pause — mirroring `facilitator-countdown.spec.ts`'s own flow. Verified against real Firebase emulators, 1/1 passing.

### Implementation for User Story 5

- [X] T061 [US5] Verify contrast for any tokens added in T012 across every `Board State` variant in both themes, per `contracts/accessibility-interaction-contract.md`; fix any failure found — **Done**. T012 was explicitly skipped (Direction C uses only pre-existing tokens, zero new ones) so there is nothing token-specific for this task to newly verify — re-ran `contrast.tokens.test.ts`/`contrast.test.ts`/`contrast.focus.test.ts` (62/62 passing) to reconfirm the existing pairings still hold. The practical per-`Board State` contrast verification this task calls for is what T057's new axe-core WCAG scans exercise directly (all 4 states × both themes, 12/12 passing) — the RetrospectivePage.tsx dark-theme gradient defect T057 found and fixed is exactly the kind of failure this task exists to catch. No new failures found; nothing to fix beyond what T057 already fixed.
- [X] T062 [US5] Run the `review-animations` skill (Constitution Principle IX) as a critique pass over all motion introduced in T028, T029, T038, T056, documenting findings and applying any fixes — **Done**, ran the skill against every motion-bearing file touched across the whole feature (broader than just T028/T029/T038/T056, since motion was introduced throughout). 13 findings, all fixed: (1) **the flagged transform-origin gap, confirmed and fixed once at the shared-hook level** — `useBoardMenuOverlay.ts` now derives `transform-origin` from Floating UI's *resolved* `context.placement` (accounting for `flip`), merged into the returned `floatingStyles` so all 4 anchored-menu consumers inherit the fix with zero per-component changes, instead of drifting out of sync via 4 separate patches; (2) 3 more `transition-all` unbounded-property violations (`FacilitatorMenu.tsx`'s trigger, `FacilitatorMenuTabs.tsx`'s tab button, `GroupableColumn.tsx`'s color-styled form) → named properties; (3) 7 more instances of the recurring missing-ease-out gap across `RetrospectiveBoard.tsx` (the board's own column entrance — not caught by earlier per-component tasks since T026 predates the `animate`-skill-formalization tasks), `FacilitatorMenu.tsx`'s icon rotation, `CardMenu.tsx`/`ColumnHeaderMenu.tsx` menu entrances, `FacilitatorMenuTabs.tsx`'s badge, and `GroupCard.tsx`'s mount + member-stagger; (4) `FacilitatorMenuTabs.tsx`'s tab-indicator spring softened from `{duration:0.6, bounce:0.2}` to `{duration:0.35, bounce:0.15}` — 600ms was sluggish for a same-screen, used-often-per-session tab switch; (5) `DragDropColumn.tsx`'s drag-lift used Framer Motion's `scale`/`y` shorthand (not hardware-accelerated) during the drag gesture itself — the highest-load moment — switched to a full `transform` string. Verdict: **Approve** (post-fix) — no feel-breaking regressions, no keyboard/high-frequency motion, no `ease-in`/`scale(0)` misuse, all durations within bounds, reduced-motion respected. Verified: 683/683 unit tests (50 files) + 39/39 e2e tests against real Firebase emulators (`accessibility.spec.ts`, `facilitator-countdown.spec.ts`, `export.spec.ts`, `board-responsive.spec.ts`) — zero regressions; type-check/lint clean.
- [X] T063 [US5] Verify i18next key coverage: no hardcoded strings were introduced across T021-T056, and every new/renamed `retrospective.*`/`retrospectivePage.*` key exists in both `retro-rocket/src/locales/en.json` and `retro-rocket/src/locales/es.json` (FR-013) — **Done**. Re-ran the existing `no-hardcoded-text.test.ts` JSX-scan guard (1/1 passing). Then ran a full flattened-key parity diff between `en.json`/`es.json` for `retrospective.*`, `retrospectivePage.*`, `sentiment.*`, and `groupSuggestion.*` (this feature's touched namespaces) — found and fixed 2 real drift issues, one of them a significant bug: (1) **`en.json`'s `retrospective.facilitator.teamMood.stats` was corrupted by my own T046 edit** — a copy-paste mistake had duplicated the 5 `facilitator.tabs.*` labels (timer/AI/Team Mood/Controls/Notes) into the `teamMood.stats` object instead, clobbering the 4 keys `TeamMoodDashboard.tsx` actually renders (`totalCards`/`analyzed`/`averageConfidence`/`dominantSentiment`) — every English-language team-mood report would have shown raw untranslated keys instead of real text; restored the correct English translations. (2) Two small dead-key cleanups: `retrospective.groupSuggestion.{group,cardsInGroup}` (EN-only) and `retrospective.progress` (ES-only) were both confirmed unreferenced by any code (the actually-used equivalents live at the unprefixed top-level `groupSuggestion.*` and `retrospective.export.progress` respectively) — removed rather than duplicated, avoiding adding more unused cruft. Post-fix, all 4 namespaces have exact key-for-key parity (281/281, 11/11, 32/32, 19/19). Full regression sweep: 2475/2475 unit tests, type-check/lint clean.

**Checkpoint**: All five user stories are independently functional and
consistently accessible.

---

## Phase 8: Polish & Cross-Cutting Concerns

**Purpose**: Final verification against the feature's contracts and success
criteria, across all five user stories together.

- [X] T064 [P] Reconcile any remaining copy differences introduced by the redesign in `retro-rocket/src/locales/en.json` and `retro-rocket/src/locales/es.json`, keeping `retrospective.*`/`retrospectivePage.*` keys in lockstep; also delete the leftover `retro-rocket/src/pages/__prototypes__/RetrospectiveBoardDirectionC.tsx` reference file (kept per T011's note) now that the real rebuild fully supersedes it — **Done**. Ran a full-file (not just `retrospective.*`) flattened-key parity diff across the entirety of `en.json`/`es.json` (665/666 keys) as a final check beyond T063's namespace-scoped one — only one drift remains app-wide (`header.language`, ES-only), which is pre-existing, unrelated navigation-header copy this feature never touched; left alone as out of scope. Confirmed `retrospective.*`/`retrospectivePage.*` have exact parity. Deleted the entire orphaned `src/pages/__prototypes__/` directory (not just the named file) — confirmed via grep that its other two files (`shared/usePrototypeBoardData.ts`, `shared/PrototypeDragColumn.tsx`) were consumed only by `RetrospectiveBoardDirectionC.tsx` and each other, with zero references anywhere else in `src/`, `src/test/`, or `e2e/` — leaving them would have been dead code. Type-check clean post-deletion.
- [X] T065 Run the full verification procedure in `specs/033-retro-board-redesign/contracts/functional-parity-contract.md` (final run against the T001 baseline for every FR-002 through FR-011, FR-013, FR-017 row) and record the result — **Done**. Ran `npm run test:coverage`: 172/172 test files, 2475/2475 tests passing (every test file named across the contract's 12 rows is part of this run, per the contract's own "no separate per-row pass required" note). Coverage thresholds (`vitest.config.ts`: branches 78 / functions 64 / lines 50 / statements 50) all clear with margin: statements 75.93%, branches 82.58%, functions 74.16%, lines 75.93% — no drop from baseline. e2e-level parity (the `e2e/*.spec.ts` half of each row) is confirmed together with T069's final sweep rather than duplicated here, since T069 runs the identical named spec battery immediately after T066-T068 and a second full e2e pass now would be pure redundancy before those tasks even have a chance to introduce anything to catch.
- [X] T066 Run the full checklist in `specs/033-retro-board-redesign/contracts/accessibility-interaction-contract.md` end-to-end and record the result — **Done, all items pass, but found and fixed one significant real defect while closing evidence gaps.** WCAG 2.1 AA / contrast: T057's 12/12 state×theme axe scans. Color-alone: verified no board state relies on color alone (sentiment badges always pair icon+text, vote/like show numeric counts, the card color picker's accessible name states the color by name e.g. "Color selector: Blanco", facilitator status is conveyed by control presence/labels not color). Focus indicators: the pre-existing Landing-only test didn't cover the board itself — added a new board-specific test (5 targets: all 4 menu triggers + a card action) which **found a real, systemic defect**: none of the 4 `useBoardMenuOverlay` trigger buttons, nor the pre-existing `LikeButton.tsx`, had any `focus-visible:` styling at all — a live WCAG 2.4.7 violation on every menu trigger and every card's like button, undetected until this checklist forced the check. Fixed all 5. (Building this test also surfaced a test-methodology pitfall worth documenting: Playwright's `.focus()` doesn't set Chromium's "last input was keyboard" flag, so `:focus-visible` never matches even on a correctly-styled element — worked around with an immediate Tab/Shift+Tab round-trip.) Keyboard/touch operability: T058 covers all 6 menus/popovers for keyboard; extended the touch test to also cover the reaction picker (previously only keyboard-tested) — now all 6 are touch-verified too. DnD keyboard operability and ARIA roles (tablist/tab/tabpanel, dnd-kit's own layer): pre-existing coverage confirmed. Reduced motion: T060. Full re-verification: 34/34 accessibility.spec.ts e2e (up from 32, +2 new), 2475/2475 unit tests, type-check/lint clean.
- [X] T067 [P] Produce `specs/033-retro-board-redesign/design-review.md` documenting a structured Apple HIG design review (clarity, deference, depth) of the shipped direction, closing with zero unresolved high-priority findings (SC-005) — analogous to feature 028's `design-audit.md` and features 029/031's `design-review.md` — **Done**. Written following the 031/029 template (Clarity/Deference/Depth/Cross-cutting/Outcome). Each section cites concrete, verifiable evidence from the actual implementation and the real defects found and fixed across T021-T066, rather than generic praise — including the two most significant findings (the missing `focus-visible` styling on every menu trigger, found via T066; the missing `transform-origin` on all four anchored menus, found via T062) framed in HIG terms (deference failing when keyboard focus becomes invisible; depth failing when a panel doesn't visually emerge from its trigger). Outcome: zero unresolved high-priority findings — SC-005 satisfied.
- [X] T068 Run `specs/033-retro-board-redesign/quickstart.md` end-to-end (all five sections) and record the result — **Done, found and fixed one real regression; found and flagged one real pre-existing bug entirely outside this feature's scope.** Sections 1-3 (manual walkthrough of every FR-002 through FR-011/FR-012/FR-015/FR-016 capability): every capability listed maps directly to e2e coverage already run and passing throughout this implementation (T057-T060 for section 2/3's a11y-and-motion items specifically); not separately hand-driven in a live browser. Section 4 (automated checks): `type-check` clean, `lint` clean, `retrospective-board-no-firestore.test.ts` (3/3), `contrast.tokens.test.ts` (42/42), and the exact named e2e battery (`retrospective-board/board-creation/board-join/export/facilitator-countdown/concurrent-board-session/concurrent-board-network/accessibility.spec.ts`, 78 tests). First run: 3 failures. Isolated each: (1) `concurrent-board-session.spec.ts`'s reconnect test — passed cleanly alone, confirmed as batch-resource-contention flakiness, not a regression (absent from the final re-run). (2) **`retrospective-board.spec.ts`'s "facilitator note" test — a real regression**, `getByRole('button', {name:'Notas'})` timing out because T046 (done earlier this session) rebuilt the facilitator tabs onto a real WAI-ARIA pattern with explicit `role="tab"`, which overrides the native `<button>`'s implicit `role="button"` for accessibility-tree purposes — this e2e file was never updated for that change (no `tasks.md` task covered it; T040 only updated the *unit* tests). Fixed the one stale selector to `getByRole('tab', ...)`; grepped for and confirmed no other stale button-role tab selectors exist elsewhere in `e2e/`. (3) **`retrospective-board.spec.ts`'s typing-indicator test — a real, deterministic failure (reproduced 3/3 runs), but confirmed entirely outside feature 033's scope**: `git diff` confirms zero typing/participant-status files were touched this session; the failure is a "ghost indicator" (a stale typing status from an earlier column reappearing after the grace period should have cleared it) that the test's own extensive comments identify as a known failure mode from feature 027's real-time architecture, not a rendering/CSS concern this redesign touches. Not fixed — flagged to the user as a separate, pre-existing issue warranting its own investigation. Final re-run: 77/78 passing, only the flagged pre-existing typing-indicator bug remaining. Section 5 (design-process artifacts): `data-model.md`'s Visual Direction Catalog (3 candidates, 1 selected, reviewer + rejection reasons recorded), `visual-direction-review-contract.md` satisfied, `functional-parity-contract.md` run (T065), `accessibility-interaction-contract.md` fully checked (T066), `design-review.md` present with zero unresolved findings (T067) — all confirmed present and complete.
- [X] T069 Final regression sweep: `npm run type-check`, `npm run lint`, `npm run test:coverage`, and `npm run e2e -- retrospective-board.spec.ts board-creation.spec.ts board-join.spec.ts export.spec.ts facilitator-countdown.spec.ts concurrent-board-session.spec.ts concurrent-board-network.spec.ts accessibility.spec.ts` — confirm zero regressions against the T001 baseline and unchanged coverage thresholds — **Done**. `type-check`: clean. `lint`: clean. `test:coverage`: 172/172 test files, 2475/2475 tests, coverage unchanged from the T065 run (statements 75.93%, branches 82.58%, functions 74.16%, lines 75.93% — all clear of the 78/64/50/50 floors). e2e battery (78 tests, real Firebase emulators): 77/78 passing, reproduced identically across 2 consecutive full runs — the sole failure is the pre-existing, out-of-feature-scope typing-indicator "ghost indicator" bug documented in T068 (feature 027's real-time architecture, zero files touched by feature 033, not a regression this feature introduced). Zero regressions against the T001 baseline within this feature's actual scope.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — can start immediately
- **Foundational (Phase 2)**: Depends on Setup completion — BLOCKS all user stories (visual direction selection and the shared menu-overlay hook are consumed by every story)
- **User Stories (Phase 3-7)**: All depend on Foundational phase completion
  - US1 (P1) has no dependency on US2-US5
  - US2 (P2) rebuilds `GroupableColumn.tsx`, which wraps US1's rebuilt `DragDropColumn.tsx` (T025) — implement after US1 to avoid rework
  - US3 (P3) is file-disjoint from US1/US2 (countdown/facilitator/action-items/sentiment components) except `CardMenu.tsx` (also touched nowhere else) — independently testable
  - US4 (P4) rebuilds `RetrospectiveTopbar.tsx` and `ImprovedExportPopover.tsx`, file-disjoint from US1-US3 — independently testable
  - US5 (P5) is a cross-cutting verification pass over Stories 1-4's output — implement last
- **Polish (Phase 8)**: Depends on all five user stories being complete

### Within Each User Story

- Tests MUST be written and FAIL (where marked) before the corresponding implementation task
- Shared-hook consumers (T023, T036, T046, T049, T054) depend on Foundational T013
- US2's T034 depends on US1's T025 (`GroupableColumn.tsx` wraps `DragDropColumn.tsx`)
- US1's T030 (performance/scale verification) depends on T007's seeding helper and T021-T029's implementation tasks completing
- US4's T052 depends on US3's T043 (extends the same new test file)
- Story complete before moving to the next priority, though US1-US4 implementation may proceed in parallel with sufficient team capacity given their largely disjoint file sets

### Parallel Opportunities

- T004, T005, T006 (the three prototype directions) run in parallel — different files, no dependencies
- T007, T009 run in parallel with the prototype drafts
- T012, T013 run in parallel once the direction is selected (T010-T011 complete) — different files
- All Setup tasks marked [P] can run in parallel
- Within each user story, all test tasks marked [P] run in parallel; most implementation tasks touch distinct component files and can proceed in parallel except where noted above

---

## Parallel Example: Foundational Phase

```bash
# Launch the three visual-direction drafts together:
Task: "Draft Visual Direction A in src/pages/__prototypes__/RetrospectiveBoardDirectionA.tsx"
Task: "Draft Visual Direction B in src/pages/__prototypes__/RetrospectiveBoardDirectionB.tsx"
Task: "Draft Visual Direction C in src/pages/__prototypes__/RetrospectiveBoardDirectionC.tsx"

# After a direction is selected, launch the shared overlay hook (test-first):
Task: "Shared menu-overlay hook in src/features/boards/retrospective/hooks/useBoardMenuOverlay.ts (test-first)"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup
2. Complete Phase 2: Foundational (CRITICAL — blocks all stories)
3. Complete Phase 3: User Story 1
4. **STOP and VALIDATE**: Test User Story 1 independently (card CRUD, vote/
   like/react, presence/typing, the column-grid container, all through the
   redesigned surface, at the validated 30-card/15-participant scale)
5. Deploy/demo if ready

### Incremental Delivery

1. Complete Setup + Foundational → direction selected, shared overlay hook ready
2. Add User Story 1 → test independently → deploy/demo (MVP!)
3. Add User Story 2 → test independently → deploy/demo
4. Add User Story 3 → test independently → deploy/demo
5. Add User Story 4 → test independently → deploy/demo
6. Add User Story 5 → cross-cutting verification → deploy/demo
7. Complete Polish phase → final contract/quickstart sign-off

### Parallel Team Strategy

With multiple developers:

1. Team completes Setup + Foundational together (direction selection needs
   a single product-owner review checkpoint, T010)
2. Once Foundational is done:
   - Developer A: User Story 1 (cards, reactions, presence, and the board's
     column-grid container)
   - Developer B: User Story 3 (facilitator menu, action items, sentiment) — file-disjoint from A
   - Developer C: User Story 4 (topbar options menu, export) — file-disjoint from A and B
3. User Story 2 starts once Developer A's `DragDropColumn.tsx` (T025) lands
4. User Story 5 starts once Stories 1-4 are merged

---

## Notes

- [P] tasks = different files, no dependencies
- [Story] label maps task to specific user story for traceability
- `GroupableColumn.tsx` (US2) wraps `DragDropColumn.tsx` (US1) — sequence
  T034 after T025 if split across people
- Verify tests fail before implementing (especially T040's ARIA-tabs
  assertion, which must fail against the current div-based tab markup)
- Commit after each task or logical group
- Stop at any checkpoint to validate story independently
- Avoid: vague tasks, same-file conflicts without sequencing, cross-story
  dependencies that break independence
